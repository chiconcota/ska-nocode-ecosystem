# [ARCHITECTURAL PILLARS - BÀN GIAO PHASE 1 & 2]
> Đây là các nguyên tắc kiến trúc cốt lõi hiện hành (Current Primitives) đúc kết từ Phase 1 và 2. Chúng đóng vai trò giữ hệ thống ổn định và ngăn AI phá vỡ quy tắc, nhưng HOÀN TOÀN CÓ THỂ PIVOT (đập đi xây lại/thay đổi) bất cứ lúc nào nếu tầm nhìn và trải nghiệm thực tế của bạn lên tiếng. Quyền định đoạt tối thượng luôn thuộc về bạn!

- **1. Kiến Trúc "The Core Four" & Nền Tảng Agnostic:** Chuyển dịch từ Page Builder sang App Builder Framework. Sử dụng WordPress làm máy chủ tạm thời (Authentication & Admin UI). Hệ thống chia rẽ 4 giới hạn (Decoupled Boundaries): Ska Theme (Zero CSS), Ska No-code Design (Thiết kế & Biên dịch Tailwind JIT), Ska Data Pro (Dữ liệu), và Ska Logic Engine (Luồng sự kiện). Giao tiếp chéo qua do_action và  pply_filters, cực hạn việc gọi Class trực tiếp.
- **2. The Trinity Data Model (Ska Data Pro):** Khai tử triệt để mô hình EAV (wp_postmeta) lề mề của WordPress. Mọi Smart Object/App phải được khởi tạo bằng Bảng Phẳng (Flat Tables: ska_data_*) với Native JSON type cho multi-select/arrays. Blueprint (JSON) định tuyến cấu trúc bằng Relative Slugs (Ký danh nội bộ) và Auto-grouping để xử lý Collision an toàn.
- **3. Cỗ Máy Tailwind JIT CSS & Clean Slate:** Server-side compiler với khả năng parse linh hoạt từ Tailwind v3 đến V4 pseudo-classes (group-has-checked, [-offset]). Tuyệt đối bảo vệ **Clean Slate** (Mâm xôi thuần khiết): Không dùng !important tùy tiện, dọn sạch CSS rác của WordPress bằng Specificity Scope (:where(),  ody .ska-builder), trả toàn quyền định dạng class cho người dùng tạo Atomic Component.
- **4. SkaFX DSL & Universal Binding (Ska Logic Engine):** Thống nhất Data Fetch và Logic Rule vào 1 cỗ máy biểu thức duy nhất (SkaFX). Bất kể giao diện nào cũng hỗ trợ Nội suy dữ liệu {{...}}. Cache toàn bộ Cấu trúc Bảng ska_data_dictionary vào RAM tĩnh của PHP (thời gian quét Suffix <1ms) để hỗ trợ Định danh ngữ cảnh siêu mượt mà.
- **5. Native Backend Integration:** Hệ thống quản lý của người dùng (App Portals) sử dụng Chung giao diện Unified Canvas với thẻ Tailwind, nhưng bảo mật qua cờ publicly_queryable = false. Bất cứ API tương tác nào từ Frontend đều trả về dữ liệu bảo vệ kỹ lưỡng bằng Nonce và Data Healing (Cứu thương mảng Array bị lỗi).

## 2026-05-24 - 🟢 Hoàn thành: Rà quét bảo mật và Làm sạch lịch sử Git (Open-source Preparation)
- **Decision (API Keys Verification):** Thực hiện rà quét mã nguồn và xác nhận không có API Key nhạy cảm nào bị hardcode. API Key của Gemini được quản lý an toàn qua Options API của WordPress (`ska_gemini_api_key`), đảm bảo tính bảo mật khi công khai mã nguồn.
- **Decision (Git History Cleaning):** Sử dụng công cụ `git-filter-repo` để xóa vĩnh viễn các tệp tin tạm và nhạy cảm khỏi toàn bộ lịch sử commit, bao gồm: `wp-content/debug.log`, `ska-no-code-design.zip`, các file `diff.txt`, `test_output.txt`, và các file zip/log cũ khác làm phình dung lượng.
- **Decision (Git Garbage Collection):** Thực thi dọn rác Git tối ưu (`git gc --prune=now --aggressive`) sau khi viết lại lịch sử để giải phóng hoàn toàn dung lượng lưu trữ cục bộ của repository.

## 2026-05-24 - 🟢 Hoàn thành: Kết nối GitHub bằng SSH Key và Chuẩn hóa Git Line Endings
- **Decision (Establish SSH Connection):** Do GitHub đã chính thức khai tử xác thực bằng mật khẩu tài khoản trực tiếp qua giao thức HTTPS (báo lỗi `Password authentication is not supported`), hệ thống chuyển đổi hoàn toàn địa chỉ remote sang giao thức SSH (`git@github.com:chiconcota/ska-nocode-ecosystem.git`).
- **Decision (SSH Key Pair Generation):** Tạo mới một cặp khóa SSH an toàn theo chuẩn Ed25519 (`id_ed25519`) và hỗ trợ người dùng tích hợp Public Key vào cấu hình tài khoản GitHub cá nhân để xác thực không cần mật khẩu.
- **Decision (Git User Identity Configuration):** Thiết lập cấu hình toàn cục `user.name` là `chiconcota` và `user.email` là `lytatthanh@gmail.com` dựa trên thông tin lịch sử commit để đảm bảo tính nhất quán của dữ liệu đóng góp (contributions).
- **Decision (Local Line Endings Normalization):** Cấu hình `core.autocrlf` thành `input` để thích ứng với môi trường điều hành Linux mới. Thực hiện làm sạch và khôi phục chỉ mục cache (`git rm --cached` và `git reset --hard`) để giải quyết triệt để lỗi Git báo đổi dòng (CRLF sang LF) trên toàn bộ dự án, đưa thư mục làm việc về trạng thái sạch sẽ.
- **Decision (Clean Credential Helper):** Gỡ bỏ cấu hình `credential.helper store` và tệp chứa thông tin mật khẩu tạm thời `~/.git-credentials` để bảo vệ an toàn bảo mật, tránh rò rỉ thông tin cá nhân.

## 2026-05-23 - 🟢 Hoàn thành: Tái cấu trúc menu admin, ẩn trang logic phụ và thiết lập quy tắc i18n quốc tế hóa (Phase 4.6)
- **Decision (Admin Navigation Reordering):** Tái sắp xếp lại hệ thống menu của Ska Ecosystem trong sidebar WordPress. Đưa `Theme Options` (Design Tokens) lên vị trí số 1 (priority 1), `Ska Organisms Manager` lên số 2 (priority 2) và `Theme Builder` lên số 3 (priority 3), tất cả đều quy thuộc trực tiếp về menu cha `Ska Ecosystem`.
- **Decision (Resolve Theme Options 403 Redirect Access):** Đổi priority của hook đăng ký `admin_menu` từ 10 lên 20. Việc này giải quyết lỗi phân quyền (bị WordPress chặn hoặc báo không được phép truy cập) khi người dùng truy cập trực tiếp URL trang Options, đảm bảo menu cha `ska-system-dashboard` được đăng ký xong trước khi các submenu con trỏ vào nó.
- **Decision (Rename Admin Menu Labels):** Thay đổi các nhãn tiếng Việt còn sót và làm rõ ngữ nghĩa: đổi `Ska Data` -> `Manage Database`, `Access Database` -> `Manage Database`, `Manage Semantic IDs` -> `Workspace`, `Designer submission` -> `Ska Organisms Manager`.
- **Decision (Hide Logic Submenu Pages):** Thay đổi parent slug của toàn bộ các trang con quản trị workflow của Logic Engine thành `null` khi gọi `add_submenu_page()`. Việc này ẩn các đường dẫn phụ khỏi sidebar WordPress để tránh rác giao diện, trong khi các router động của trang vẫn truy cập bình thường qua dashboard.
- **Decision (Global AI i18n Rules):** Cập nhật quy chuẩn `.agent/rules/wp-architect.md` bổ sung điều khoản bắt buộc AI viết code bằng tiếng Anh, tự động bọc chuỗi trong hàm i18n WordPress chuẩn, chỉ cho phép bình luận (comments/PHPDoc) bằng tiếng Việt.
- **Decision (Recompile translation catalogs):** Đồng bộ hóa bản đồ dịch thuật song ngữ, dịch thêm các nhãn menu mới sang tiếng Anh và biên dịch lại tệp `.po` và `.mo` nhị phân của cả 3 plugin (`ska-no-code-design`, `ska-data-pro`, `ska-logic-engine`), đảm bảo không có nhãn tiếng Việt thô cứng trong code.

## 2026-05-23 - 🟢 Hoàn thành: Quốc tế hóa (i18n) và Khắc phục lỗi upload Logo/Font trên trang Design Tokens (Phase 4.6)
- **Decision (Design Tokens Internationalization):** Refactor toàn bộ trang Design Tokens (`design-tokens-app.php`) để chuyển đổi các chuỗi nhãn tĩnh và nhãn mảng tabs của Alpine JS sang i18n PHP chuẩn.
- **Decision (Resolve JS Compiler Crash):** Loại bỏ triệt để lỗi parse cú pháp JS/Alpine (`Unexpected token '}'` hay `Unexpected token ')'` do chuỗi raw placeholders không đóng gói), giúp trình giả lập Alpine compile component thành công và mở lại liên kết click sự kiện upload.
- **Decision (Uploader Instance Caching):** Cấu trúc lại phương thức gọi `wp.media` trong các hàm `openLogoUploader` và `openFontUploader` (đổi tên từ `openMediaUploader`). Gán trực tiếp uploader instance lên trạng thái Alpine component (`logoUploaderInstance`, `fontUploaderInstance`) thay vì khai báo biến local scope, giải phóng và ngăn chặn rò rỉ bộ nhớ (memory leaks) khi click liên tục.
- **Decision (Language Compilation Sync):** Cập nhật cơ sở dữ liệu dịch thuật song ngữ (`translation_map.json` và `vietnamese_strings.json`), tự động biên dịch lại file `.po` và `.mo` Việt - Anh của plugin `ska-no-code-design` (tổng số chuỗi dịch nâng lên 415 chuỗi).

## 2026-05-23 - 🟢 Hoàn thành: Quốc tế hóa (i18n) và sửa lỗi cú pháp Alpine JS trên trang Design Workspace (Phase 4.6)
- **Decision (Wrap Design Workspace labels in i18n):** Tiến hành bọc toàn bộ các nhãn UI tiếng Việt còn sót lại trên trang Design Workspace (file `workspace-panel.php`) vào các hàm WordPress i18n PHP chuẩn (`esc_html_e`, `esc_attr`, `esc_js`).
- **Decision (Fix placeholder and text interpolation syntax error):** Loại bỏ cách viết gọi trực tiếp hàm JS `__( ... )` không qua bọc chuỗi (dẫn tới lỗi parser và runtime crash của Alpine JS). Thay thế bằng các block code PHP sạch: `placeholder="<?php echo esc_attr( ... ); ?>"` và `<span><?php esc_html_e( ... ); ?></span>`.
- **Decision (Compile translation files):** Đồng bộ hóa bản đồ dịch thuật song ngữ (`translation_map.json` và `vietnamese_strings.json`), tự động biên dịch lại file `.po` và `.mo` Việt - Anh của plugin `ska-no-code-design` (tổng số chuỗi dịch nâng từ 390 lên 393 chuỗi).

## 2026-05-23 - 🟢 Hoàn thành: Quốc tế hóa (i18n) hoàn toàn các nhãn UI và tuỳ chọn điều kiện hiển thị trên Dashboard Theme Builder (Phase 4.6)
- **Decision (Wrap remaining Vietnamese labels in i18n):** Tiến hành rà soát và bọc toàn bộ các nhãn UI hiển thị bằng tiếng Việt thô còn lại trên Dashboard Theme Builder (ở file `admin-panel.php`) vào các hàm WordPress i18n chuẩn: `esc_html_e()` cho HTML thô và `esc_js( __( ... ) )` cho các nhãn nằm trong cấu trúc dữ liệu JSON/Javascript của AlpineJS (các nhãn như `Tất cả App`, `Tạo Template`, `Cập nhật:`, `Mở Editor`, `Sửa Settings`, `Xóa Template`, `Thêm Rule`, `Hủy`, `Lưu`, v.v.).
- **Decision (Bidirectional translation syncing):** Đăng ký đầy đủ các chuỗi tiếng Việt thô và các bản dịch tiếng Anh kỹ thuật tương ứng vào cơ sở dữ liệu map dịch thuật song ngữ (`translation_map.json` và `vietnamese_strings.json`). Chạy công cụ Node compiling để tái tạo và đồng bộ hóa tệp dịch nguồn `.po` và tệp nhị phân `.mo` bản ngữ Việt - Anh, tăng tổng số chuỗi dịch trong `ska-no-code-design-vi.po` từ 377 lên 390 chuỗi, bảo đảm giao diện song ngữ hoạt động trơn tru 100%.

## 2026-05-22 - 🟢 Hoàn thành: Khắc phục lỗi ẩn ô nhập Toast Message của Client Response Node (Phase 4.6)
- **Decision (Client Response Dropdown & Visibility Bug):** Khắc phục lỗi Settings Panel không hiển thị ô nhập `Toast Message` và `Toast Type` khi node có `response_type` là `remove_row` (loại phản hồi xóa dòng dữ liệu tự sinh qua CRUD Workflow). Do giá trị `remove_row` không nằm trong các tùy chọn mặc định của dropdown và điều kiện lọc chỉ chấp nhận hiển thị khi `response_type === 'toast'` hoặc trống. Đã bổ sung option `remove_row` vào dropdown select trong `SettingsPanel.jsx` và mở rộng biểu thức kiểm tra hiển thị bao gồm `response_type === 'remove_row'`.

## 2026-05-22 - 🟢 Hoàn thành: Refactor và Tích hợp Tính năng Xóa Dòng trên Portal List View qua Logic Engine Workflow (Phase 4.6)
- **Decision (Workflow-based Deletion):** Chuyển đổi tính năng xóa dòng dữ liệu trên Portal List View từ gọi API REST thô kèm script inline sang cơ chế Workflow của Ska Logic Engine. Nút Xóa sinh ra tự động liên kết với một action class dạng `ska-action-delete_{table_slug}` và context payload `data-ska-payload="{{id}}"` để tự động kích hoạt workflow API submit.
- **Decision (Auto-registered Deletion Workflows):** Nâng cấp bộ máy `Form_Receiver` tự động đăng ký một workflow CRUD xóa bản ghi dạng `delete_{table_slug}` dưới cơ sở dữ liệu phẳng (`wp_ska_data_*`) khi nhận được yêu cầu xóa dòng lần đầu tiên, giúp giảm cấu hình thủ công cho Power User.
- **Decision (Avoid Nesting Tag Conflict):** Refactor cấu trúc trong `Ska_Portal_Generator` để chuyển đổi container bọc ngoài của Row từ thẻ `<a>` sang thẻ `<div>` khi render ở editor/preview. Sử dụng sự kiện click điều hướng ở mức cha nhưng tránh xung đột lồng thẻ, cho phép nút Xóa (thẻ `button` hoặc `a` có action class) ngăn chặn nổi bọt sự kiện click (`@click.stop` hoặc e.stopPropagation).
- **Decision (Universal confirmation & DOM removal UX):** Bổ sung thuộc tính `data-ska-confirm` tại file `ska-core.js` để tự động hiển thị hộp thoại xác nhận trước khi gửi yêu cầu workflow. Sự kiện click của nút hành động sẽ lưu trữ element vào `window.$ska.lastClickedElement`. Khi Workflow trả về Client Response có action `remove_row`, Event Bus phía JS client sẽ tìm thẻ chứa dòng gần nhất (tổ tiên của nút được bấm), kích hoạt transition CSS làm mờ (opacity 0) và thu nhỏ chiều cao (height 0, padding/margin 0) mượt mà (300ms) trước khi xóa hẳn khỏi DOM, mang lại trải nghiệm mượt mà và không giật lag.

## 2026-05-22 - 🟢 Hoàn thành: Kết nối Dữ liệu Quan hệ & Sửa lỗi Reset Form trên Portal Detail View (Phase 4.6)
- **Decision (Skip Sanitization for Long Text & JSON Columns):** Bổ sung cơ chế tự động truy xuất cấu trúc bảng (Schema Dictionary) trong class `Ska_Logic_Form_Receiver`. Nếu cột dữ liệu có kiểu là `long_text` hoặc kiểu dữ liệu JSON (`relation`, `multi_select`, `rollup`), hệ thống sẽ bỏ qua hàm `sanitize_text_field()` vốn cắt xén các comment của Gutenberg (`<!-- wp:ska-builder/text -->`) và các mã HTML/Tailwind phức tạp (bao gồm các block như `html2tailwind`), thay vào đó sử dụng `wp_unslash()` để lưu trữ dữ liệu thô nguyên vẹn vào bảng phẳng `wp_ska_data_*`. SQL Injection vẫn được ngăn chặn 100% nhờ việc sử dụng prepared statements (`$wpdb->insert` / `$wpdb->update`) ở tầng cơ sở dữ liệu.
- **Decision (Two-Way Binding for Code and Visual Editor):** Khắc phục lỗi bất đồng bộ dữ liệu của `skaScratchpad` component khi TinyMCE chưa được khởi tạo (do editor mặc định mở ở tab Code/HTML). AlpineJS Controller đã được nâng cấp để thực hiện đồng bộ hai chiều trực tiếp với thẻ `<textarea>` thô ngay khi tải trang. Đồng thời, đăng ký lắng nghe sự kiện `AddEditor` của TinyMCE để tự động thiết lập liên kết đồng bộ hai chiều ngay khi TinyMCE được khởi tạo động (ví dụ khi người dùng chuyển sang tab Visual), đảm bảo dữ liệu luôn được cập nhật chính xác bất kể chế độ editor nào hoạt động.
- **Decision (Prevent Form Reset on Update View):** Khắc phục lỗi các trường dữ liệu bị biến mất (reset về chuỗi rỗng) sau khi người dùng nhấn nút cập nhật trên Detail/Update View. Bổ sung check `isUpdate` chặt chẽ trong `ska-frontend.js` bằng cách kiểm tra: `actionId.startsWith('update_')`, sự hiện diện của `fields.id`, và xác thực `Alpine.store('skaPortal').currentData.id` (chỉ có khi đang ở view detail/update của Portal). Khi `isUpdate` là true, form sẽ giữ nguyên giá trị đã nhập, ngược lại nếu ở Create View (`isUpdate` là false), form sẽ tự động reset về rỗng để sẵn sàng cho lần nhập tiếp theo. Sử dụng dynamic version `filemtime` cho file `ska-frontend.js` trong `blocks/init.php` to giải quyết triệt để vấn đề cache JS của trình duyệt.
- **Decision (Fix List View Relation Rendering):** Nới lỏng kiểm tra chuỗi JSON trong `src/ska-loop/render.php` và `build/ska-loop/render.php`. Thay vì kiểm tra chính xác `[{"`, hệ thống sẽ kiểm tra bằng `str_starts_with($val_clean, '[{')` kết hợp với lệnh `stripslashes($val)` để dọn dẹp các ký tự escape do WordPress chèn vào trước khi giải mã JSON. Việc này giúp List View hiển thị chính xác tên nhãn của các đối tượng liên kết (ví dụ: "vung tau" thay vì hiển thị chuỗi JSON thô).
- **Decision (Fix Detail View Relation Dropdown Empty Option):** Giải quyết lỗi dropdown select bị trống trơn không có options khi được tạo tự động bởi One-Click App Generator. Trong file `class-dynamic-content.php` (phương thức `process_foreach_loop`), hệ thống đã hỗ trợ thêm trường hợp nạp danh sách options trực tiếp qua primary key của bảng đích (`{{#foreach target_table.id}}`). Khi phát hiện binding trỏ tới cột `id` của một bảng flat table, hệ thống tự động xác định đó là bảng đích, tìm cột nhãn (`display_col` kiểu varchar/text) của bảng đó và thực hiện truy vấn nạp tối đa 1000 bản ghi để làm options cho dropdown.
- **Decision (Automated Pre-selection in AlpineJS Form):** Khi dữ liệu quan hệ được nạp lên Detail View dưới dạng mảng JSON hoặc chuỗi ID, AlpineJS Controller (`skaForm` trong `ska-frontend.js`) sẽ tự động giải mã và gán ID chính xác của bản ghi liên kết vào form state (`fields.phong_dat = "1"`). Nhờ có các options được nạp đầy đủ từ backend, dropdown select sẽ tự động hiển thị đúng giá trị đã được lưu từ trước.

## 2026-05-21 - 🟢 Hoàn thành: Tinh giản Layout & Nâng cấp UX/UI Trình soạn thảo văn bản giàu (Phase 4.6)
- **Decision (Fix Shadow Scratchpad REST Route 404):** Khắc phục triệt để lỗi REST API trả về 404 (`No route was found matching the URL and request method`) khi nhấp nút "Open Design Editor" ở Frontend Portal. Phát triển hàm `getBuilderRestUrl()` trong AlpineJS component `skaScratchpad` để chuẩn hóa đường dẫn gốc của API (hỗ trợ hoàn hảo cả Pretty Permalinks lẫn Plain Permalinks với tham số `?rest_route=`), tự động ánh xạ chính xác namespace từ `ska-logic/v1/submit` sang `ska-builder/v1` và kết nối trực tiếp đến các endpoint `/scratchpad/create` và `/scratchpad/destroy`. Loại bỏ thuộc tính `display: flex !important` trong CSS của modal để tránh xung đột với logic hiển thị `x-show` của AlpineJS.
- **Decision (Fix Unsaved Changes Alert on Close):** Giải quyết triệt để lỗi cảnh báo trình duyệt ("Rời khỏi trang web? Các thay đổi bạn đã thực hiện có thể không được lưu") khi bấm nút "Hoàn tất & Cập nhật" hoặc nút đóng modal. Bên cạnh việc gọi `wp.data.dispatch('core').clearEntityRecordEdits()` ở phía JS client, hệ thống tích hợp giải pháp đánh chặn trực tiếp tại backend: thông qua hook `admin_head` dành riêng cho Custom Post Type `ska_scratchpad`, một script inline được chèn sớm vào `<head>` của trang soạn thảo để override hàm `window.addEventListener` và thuộc tính `window.onbeforeunload`, lọc và chặn hoàn toàn việc đăng ký các listener sự kiện `beforeunload`. Điều này triệt tiêu hoàn toàn hộp thoại cảnh báo khi huỷ hoặc thay đổi URL của Iframe.
- **Decision (Combine Title and Designer Button):** Loại bỏ toàn bộ các container bọc ngoài dư thừa khi sinh mã layout tự động trong `class-ska-portal-generator.php`. Khối `ska-builder/form-rich-text` hiện tại tự quản lý phần Header chứa nhãn H3 ở bên trái và nút **Open Design Editor** ở bên phải, loại bỏ hoàn toàn việc lặp nhãn "Mô tả chi tiết" và badge "Classic Editor" tĩnh.
- **Decision (Custom TinyMCE Segmented Pills Tab Styling):** Nhúng khối CSS scoped cục bộ vào khối soạn thảo để tùy biến hoàn toàn thanh tab Visual/Code mặc định của WordPress TinyMCE thành bộ điều khiển Segmented Pills cao cấp. Nút tab hoạt động trực quan với đường viền phân tách rõ ràng, các hiệu ứng hover, shadow và hiển thị trạng thái active với nền trắng nổi bật, cải thiện mạnh mẽ UX soạn thảo của người dùng.
- **Decision (Separate Save vs Close Modal Actions):** Cập nhật phương thức `closeDesigner(saveChanges)` trong tệp [ska-frontend.js](file:///c:/Users/ADMIN/Local%20Sites/ska-core-builder/app/public/wp-content/plugins/ska-no-code-design/assets/js/ska-frontend.js) để phân biệt hành vi đóng modal. Chỉ cập nhật nội dung từ Gutenberg iframe vào Alpine fields và TinyMCE khi nhấn "Hoàn tất & Cập nhật" (`saveChanges = true`). Bấm nút đóng "X" hoặc click-away bên ngoài overlay (`saveChanges = false`) sẽ hủy thay đổi. Cả hai trường hợp đều thực hiện dọn dẹp dirty edits ở client để tránh cảnh báo trình duyệt và gọi API hủy scratchpad tạm thời ở backend để tránh rác database.
- **Decision (Match Gutenberg Mockup UI):** Cập nhật tệp `index.js` của khối để đồng bộ hóa giao diện soạn thảo tĩnh trong Gutenberg, giúp Nocode Builder thấy được bố cục H3 tiêu đề bên trái, nút kích hoạt bên phải và giao diện tab hiện đại chuẩn xác.

## 2026-05-21 - 🟢 Hoàn thành: Sửa lỗi cuộn kép (double scrollbar) frontend & cắt xén block appender (+) editor
- **Decision (Bypass h-screen/overflow-y-auto layout constraints):** Loại bỏ hoàn toàn lớp `h-screen overflow-hidden` trên thẻ `<form>` và `overflow-y-auto bg-slate-50 min-h-screen` trên thẻ `<main>` khi sinh mã layout tự động (hàm `build_form_layout()` của `Ska_Portal_Generator`).
- **Decision (Native scrolling with sticky header):** Thay thế bằng `min-h-screen` trên `<form>` và `flex-1 bg-slate-50` trên `<main>`. Điều này cho phép trang app portal cuộn tự nhiên bằng scrollbar của cửa sổ trình duyệt (viewport body), triệt tiêu hoàn toàn thanh cuộn kép (double scrollbar) ngoài frontend trong khi sticky header vẫn hoạt động đúng chuẩn.
- **Decision (Prevent editor appender clipping):** Việc gỡ bỏ `overflow-hidden` ở thẻ form bọc ngoài giúp Gutenberg Editor không bị giới hạn chiều cao hiển thị và không còn cắt xén (clip) các viền outline block hoặc các nút chèn block (`+` appender) ở cuối khung soạn thảo.

## 2026-05-21 - 🟢 Hoàn thành: Thiết kế Dedicated Create View & Notion-style Form Layout (Phase 4.6)
- **Decision (Dedicated Create View):** Hủy bỏ hoàn toàn cấu trúc Modal Quick Edit cho chức năng Thêm mới bản ghi để tránh làm phình DOM và làm nặng List View. Thay vào đó, tạo một trang Dedicated Page cho chức năng thêm mới. Bổ sung hàm `create_insert_view` sử dụng mode `insert` để phân biệt với `create_detail_view` (mode `update`).
- **Decision (Notion-style Form Layout):** Refactor `class-ska-portal-generator.php` bổ sung hàm `build_form_layout` dùng chung cho cả Insert và Update. Giao diện Form được chuẩn hóa theo Notion-style (nền trắng, viền mỏng, responsive, header cố định, tiêu đề khổng lồ).
- **Decision (App Generator API Update):** Cập nhật API AJAX của App Generator để trả về thêm tham số `insert_view_editor_url`, đồng thời chỉnh sửa giao diện Success Modal (`manage-modals.php` - AlpineJS) để Nocode Admin có thể truy cập nhanh vào giao diện Theme Builder của Create View ngay sau khi sinh App.

## 2026-05-21 - 🟢 Hoàn thành: Thuật toán Cột Động (Dynamic Grid) cho Auto-Generated List View & Bản vá JIT Compiler
- **Decision (Smart Column Picker & Dynamic Grid):** Khắc phục triệt để vấn đề mỗi bảng dữ liệu có số lượng cột khác nhau dẫn đến vỡ khung hiển thị List View. Hệ thống Auto-Generator giờ đây tích hợp hàm `compute_list_grid()`. Thuật toán sẽ quét Schema, tự động loại trừ các cột dài (`long_text`, `json`, `textarea`), đếm số cột hợp lệ còn lại và sinh ra chuỗi class lưới động của Tailwind (ví dụ `grid-cols-[2fr_1fr_1fr_40px]`). Cả `create_list_view` (Header) và `create_organism` (Row Item) đều kế thừa chung chuỗi Grid này, đảm bảo giao diện luôn khớp tuyệt đối 100% với cấu trúc dữ liệu của từng App mà không cần người dùng can thiệp thủ công.
- **Decision (Tailwind JIT Compiler Fixes):** Vá 3 lỗ hổng nghiêm trọng trong `Tailwind_Compiler` khiến CSS Grid động không hoạt động: 1) Bổ sung Regex nhận diện Arbitrary Values `grid-cols-[...]`. 2) Gỡ bỏ thuộc tính `display: grid;` bị ép cứng để trả lại quyền điều khiển cho class `hidden` (Responsive). 3) Vá lỗi thiếu Escape ký tự đặc biệt (`(`, `)`, `,`) trong CSS Selector, đảm bảo các hàm `repeat()` và `minmax()` không làm hỏng cú pháp CSS. Đồng thời bổ sung lệnh gọi `export_physical_cache()` vào cuối tiến trình Generator để `organisms.json` luôn đồng bộ hóa thời gian thực.
- **Decision (Premium App Portal Aesthetics):** Nâng cấp thiết kế giao diện của List View thành một Panel nguyên khối hiện đại (Card Layout, bo tròn `rounded-xl`, nền xen kẽ `bg-slate-50`). Tích hợp Icon mũi tên `>` làm tín hiệu điều hướng, gộp ID và Tên vào cùng một cột để tối ưu không gian ngang. Tính năng "Modal Quick Edit" tạm gác lại để dồn toàn lực vào kiến trúc chuyển trang tĩnh (Dedicated Page) nhằm bảo toàn triết lý Zero-Trash.

## 2026-05-20 - 🟢 Hoàn thành: Nâng cấp Auto-CRUD Detail View Generator sang Giao diện Premium & Sửa Lỗi API Xóa Symbol
- **Decision (Theme Template Deletion Garbage Collection):** Khắc phục triệt để vấn đề tích tụ các Organisms mồ côi làm rác database khi người dùng xóa Template từ Theme Builder UI. API xóa template (`delete_template` trong `Ska_Theme_Builder_API`) hiện đã tự động truy vấn lấy `organism_id` liên kết và xóa bản ghi tương ứng trong bảng `wp_ska_data_sys_organisms` trước khi hoàn tất việc xóa template.
- **Decision (Zero-Query Organisms Cache Invalidation):** Sửa lỗi Dropdown "Chọn Symbol" trên UI (Gutenberg Editor) vẫn hiển thị các "bóng ma" Organisms mặc dù đã xóa chúng trong cơ sở dữ liệu. Nguyên nhân do tính năng tăng tốc Editor (Zero-Query) nạp dữ liệu từ tệp tĩnh `organisms.json` bằng `wp_localize_script`, nhưng các tác vụ xóa rác DB thuần túy (SQL DELETE) không trigger luồng làm mới tệp tĩnh này. Đã bổ sung logic gọi `Organisms_API::export_physical_cache()` sau mỗi hành động dọn rác DB trong `Ska_Portal_Generator` và `Ska_Theme_Builder_API` để đảm bảo UI và Database luôn đồng bộ thời gian thực.
- **Decision (Auto-Generated Templates Folder Categorization):** Khắc phục lỗi 2 view templates sinh tự động không được phân loại vào đúng App trong Theme Builder. Thay đổi cấu trúc lưu trữ `conditions` của template từ dạng mảng rules phẳng sang cấu trúc JSON Object (`{ "rules": [...], "folder_id": "..." }`) tương thích 100% với UI. Đồng thời, bổ sung logic tự động đồng bộ App của bảng dữ liệu (Ska Data Pro) sang Folder tương ứng trong Theme Builder khi sinh Portal (tự động tạo folder mới khớp với tên App nếu chưa tồn tại và gán `folder_id`).
- **Decision (Theme Builder App Portal Display Conditions):** Sửa lỗi giao diện Modal "Sửa Template" (Theme Builder UI) không nhận diện được điều kiện `specific_portal_list` và `specific_portal_detail` do One-Click App Generator sinh ra (dẫn đến việc fallback hiển thị nhầm thành "Toàn bộ trang"). Bổ sung 2 rules này vào mảng `ruleOptions` trong AlpineJS (`admin-panel.php`) cùng logic hiển thị thẻ `<input>` để người dùng có thể dễ dàng quản lý tham số URL Slug cho List View và Detail View của App.
- **Decision (Frontend Router 404 Bugfix):** Sửa lỗi người dùng gặp trang 404 khi truy cập vào link của App Portal. Nguyên nhân là do bộ One-Click Generator đã gán nhầm Tên bảng (`$table_slug`) vào giá trị của "Điều kiện hiển thị" (Display Conditions) thay vì dùng Đường dẫn Frontend (`$portal_slug`). Điều này khiến Frontend Router (Virtual Wrapper) không thể đối chiếu khớp URL người dùng gõ với Template trong cơ sở dữ liệu. Đã sửa hàm `generate_portal` trong `class-ska-portal-generator.php` để truyền trực tiếp `$portal_slug` vào `create_list_view` và `create_detail_view`, đồng thời cập nhật đúng đường link cho các nút "Hủy bỏ" và "Quay lại danh sách".
- **Decision (Garbage Collection SQL Bugfix):** Sửa lỗi SQL crash (Unknown column 'slug') khi xóa bảng dữ liệu do bảng `ska_data_sys_theme_templates` không chứa cột `slug`. Cấu trúc dọn rác mới sẽ truy quét các Template có `conditions` chứa `table_slug`, thu thập danh sách `organism_id` liên kết để xóa chúng trước, sau đó quét dọn các organism rác chứa reference đến bảng trong block content (`json_content` chứa `"sourceTable"`, `[table.id]` hoặc form `update_[table]`), và cuối cùng xóa các templates đó, trả lại CSDL sạch 100%.
- **Decision (Premium Detail View Layout):** Nâng cấp hàm `create_detail_view` trong `Ska_Portal_Generator` để tự động sinh giao diện Detail View cao cấp (Premium). Tách biệt các trường ngắn và dài: Tự động gom nhóm các trường ngắn (Input, Select, Relation) vào Grid Container `grid grid-cols-1 md:grid-cols-2 gap-6`, còn trường văn bản dài (Rich Text) được giữ ở dạng full-width, tối ưu không gian hiển thị của biểu mẫu.
- **Decision (List View Row Template Upgrade):** Khắc phục lỗi Auto-Generator sinh ra các khối Gutenberg mặc định (`core/group`, `core/paragraph`) khi tạo List View Row Template (Organism). Đã sửa hàm `create_organism` để 100% sử dụng **Ska Atomic Blocks** (`ska-builder/container`, `ska-builder/text`), giúp Nocode Builder dễ dàng định dạng bằng Tailwind JIT mà không bị báo lỗi block không tương thích. Đã chạy kịch bản tự động quy đổi toàn bộ Row Templates cũ trong CSDL sang kiến trúc block mới.
- **Decision (Standardized UI Field Styling):** Thay đổi style của Label thành `text-xs font-bold uppercase tracking-wider text-slate-500` và nâng cấp các trường Input/Select với style tinh tế hơn (`w-full px-4 py-2.5 border border-slate-200 rounded-xl text-sm focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100 transition-all outline-none bg-slate-50/50 focus:bg-white`).
- **Decision (Header Navigation & Breadcrumbs):** Bổ sung một Header Container phía trên biểu mẫu chứa nút Breadcrumb quay lại trang danh sách (sử dụng thẻ `a` với url `/{table_slug}/` and text `← Quay lại danh sách`) cùng tiêu đề `Chi tiết: {table_label}`, cải thiện mạnh mẽ khả năng điều hướng Portal.
- **Decision (Data Fetcher Security Compatibility):** Khắc phục lỗi hiển thị trắng List View sau khi sinh tự động. Lỗi xảy ra do Block `ska-builder/loop` truyền tên bảng rút gọn (`wp_app_...`) vào Data Fetcher thay vì tên bảng đầy đủ (`wp_ska_data_app_...`), khiến hệ thống bảo mật tự động chặn query. Đã cập nhật `class-ska-portal-generator.php` (hàm `create_list_view`) để luôn sử dụng `$table_name` gốc, đảm bảo tương thích 100% với kiến trúc bảo mật của Ska Data Pro.
- **Decision (Form Actions Footer):** Gom nhóm nút bấm "Hủy bỏ" (link `a` trỏ về danh sách) và nút "Lưu Thay Đổi" (submit button) nằm trên một thanh tác vụ chung ở chân biểu mẫu form (`flex items-center justify-end gap-3 pt-6 border-t border-slate-100 mt-8`), tăng độ hoàn thiện UX.
- **Decision (Gutenberg Attribute Newline Escaping):** Khắc phục lỗi JSON parser của Gutenberg Editor bằng cách tự động escape ký tự xuống dòng `\n` thành `\\n` cho thuộc tính `optionsText` trong comment JSON block. Điều này giữ nguyên các tùy chọn tĩnh (như loại phòng, trạng thái) không bị reset hoặc biến mất khi mở trong Editor.
- **Decision (Symbol Deletion API Safeguard):** Sửa lỗi PHP Deprecated warning phát sinh trong hàm `export_physical_cache` khi gọi `json_decode` trên trường `json_content` rỗng/null của Symbol. Sử dụng toán tử null coalescing (`?? ''`) ngăn chặn cảnh báo làm vỡ luồng JSON response của REST API, giúp tính năng xóa Symbol trên UI hoạt động ổn định và chính xác mà không gặp lỗi kết nối.

---
## 2026-05-19 - 🟢 Hoàn thành: Bổ sung Điều hướng Trình soạn thảo (Editor Navigation) cho App Portal Generator
- **Decision (Direct Editor Navigation):** Quyết định trả về trực tiếp `list_view_editor_url` và `detail_view_editor_url` từ `Ska_Portal_Generator::generate_assets()` thông qua API response của `class-admin-ajax.php`. Sự thay đổi này nhằm giúp người dùng có thể điều hướng (chuyển trang) nhảy thẳng vào giao diện thiết kế Gutenberg (Theme Builder) của List View hoặc Detail View ngay lập tức sau khi App Portal được sinh ra tự động.
- **Decision (App Generator Success UI):** Nâng cấp giao diện Success Modal (`manage-modals.php`) hiển thị 4 nút bấm điều hướng tĩnh (Edit List View, Edit Detail View, View Frontend, Manage Templates) dựa trên payload mới từ AJAX. Tính năng này đóng gói trọn vẹn luồng UX (Trải nghiệm người dùng) cho tính năng "Tạo App trong 1 Click", giảm thiểu số lần click chuột thừa thãi của End-User.

---
## 2026-05-18 - 🟡 Pending: Kiến Trúc Redirect Cấp Độ App (App-Level Fallback)
- **Decision (App-Level Redirect Fallback):** Thống nhất định hướng kiến trúc Redirect (chuyển hướng 302/301 khi truy cập trái phép) cho App Portal. Thay vì bắt buộc cấu hình lặp lại cho từng bảng (Table-level), hệ thống sẽ bổ sung cơ chế Kế thừa (Fallback): Ưu tiên cấu hình riêng của Table -> Trùng lặp sang cấu hình chung của App (Workspace) -> Cuối cùng fallback về `wp-login.php`. Quyết định này được đưa vào Log để lùi lịch triển khai sang **Giai đoạn Post-MVP** nhằm tối ưu hóa tính DRY (Don't Repeat Yourself) và quản trị tập trung, tránh làm chậm tiến độ lõi hiện hành.

---
## 2026-05-18 - 🟢 Hoàn thành: Kiến Trúc Universal Pattern & Shadow Scratchpad (One-Click App Generator)
- **Decision (Atomic Portal Assets Generation):** Hoàn tất xây dựng hệ thống `Ska_Portal_Generator` (Decoupled qua hook `ska_design_generate_portal_assets`). Tự động sinh `List View`, `Detail View` (Theme Templates) và các `Organisms` (như Row Data, Form Layout) nguyên tử (Atomic). Mọi asset sinh ra đều tương thích chuẩn với Ska Theme Builder, cho phép end-user tuỳ chỉnh tự do bằng trình kéo thả sau khi khởi tạo.
- **Decision (Zero-Bloat Garbage Collection):** Áp dụng kiến trúc dọn rác tự động. Các asset sinh ra được ghim lại với ID bảng dữ liệu. Bổ sung listener lắng nghe hook `ska_data_table_deleted`. Khi bảng dữ liệu bị xoá bên Data Pro, Design Engine sẽ lập tức truy quét và xoá bỏ toàn bộ Templates & Organisms liên đới, giữ cho Database luôn sạch sẽ.
- **Decision (Shadow Scratchpad Architecture):** Chốt và triển khai thành công mô hình **Iframe Cách ly (Shadow Scratchpad)** cho việc nhập liệu Rich Text (Gutenberg) ngoài Frontend. API `/scratchpad/create` tạo một Custom Post Type nháp (`ska_scratchpad`) với ID phái sinh. Sau khi User hoàn thành thiết kế và tắt Iframe, HTML được trích xuất trả về Alpine model, đồng thời gọi API `/scratchpad/destroy` để xoá sổ CPT nháp này ngay lập tức. Đảm bảo trải nghiệm thiết kế Full-site mà không gây phình to bảng `wp_posts` rác và giải quyết trọn vẹn bài toán Data Concurrency.


---
## 2026-05-18 - 🟡 Pending: Kiến trúc Role-Based Access Control (RBAC) Tầng Hiển thị
- **Decision (RBAC Deferral):** Quyết định tạm hoãn tính năng phân quyền hiển thị (RBAC - ẩn/hiện Block/Template dựa trên role người dùng) ở mức Theme Builder sang giai đoạn Post-MVP. Hiện tại Middleware (Data Pro & App Router) đã đủ sức bảo vệ an toàn luồng dữ liệu Backend (trả về 403 nếu không đủ quyền). Việc lùi RBAC UI giúp dồn toàn lực tập trung xây dựng tính năng lõi **Auto-Generated CRUD (Macro Injector)** mang tính "Wow factor" và giải quyết trực tiếp pain-point lớn nhất của người dùng No-code.

---
## 2026-05-17 - 🟢 Sửa lỗi App Portal Custom Redirect & Hoàn thiện Data Pipeline
- **Decision (Frontend Build Pipeline Sync):** Xác nhận nguyên nhân lỗi App Portal Redirect trả về trang mặc định `wp_login_url()` là do thiếu bước biên dịch (build) bộ Javascript `schema.js` ở phía Frontend, khiến tham số `unauthorized_redirect_url` không được truyền về Backend để lưu vào Database. Đã tiến hành bổ sung `npm run build` vào quy trình để đồng bộ hoàn toàn cấu hình UI mới vào `admin-datagrid.bundle.js`.
- **Decision (Middleware Enforcement):** Xác nhận lại kiến trúc `Ska_App_Router::enforce_security` hoạt động đúng 100%. Khi tham số `unauthorized_redirect_url` có mặt trong cấu hình Portal (`ska_data_dictionary`), hệ thống sẽ thực hiện redirect 302 chính xác và truyền kèm biến `?redirect_to=` tới trang Lead Page/Custom Page, thay vì fallback về trang đăng nhập WordPress. Lỗi đã được triệt tiêu hoàn toàn. Đã khởi tạo file hướng dẫn End-User QA tại `qa_workflow_phase5.md`.

---
## 2026-05-17 - 🟢 Hoàn tất E2E Testing Dedicated Pages (Milestone 6) & Bugfix Deprecation

---
## 2026-05-16 - 🔴 Pivot: Chuyển dịch từ SPA App Portal sang Dedicated Pages & Tối Ưu Hiệu Năng
- **Decision (Dedicated Page Architecture):** Phế bỏ hoàn toàn kiến trúc SPA (Single Page Application) sử dụng `x-show` cho App Portal. Thay vào đó, chuyển sang mô hình **Dedicated Page** (Định tuyến theo URL). Mỗi view (List, Detail, Create, Edit) sẽ là một Template riêng biệt (CPT `ska_theme_builder`). Điều này giúp tránh DOM phình to và rối rắm cho Nocode Admin.
- **Decision (App Categorization - JSON Dictionary):** Không tạo Taxonomy và không lưu meta thuần túy rải rác để tránh N+1 Query. Quyết định lưu cấu trúc **Virtual Folder** (danh mục App) thành một mảng JSON Dictionary tại `wp_options`. Khi load Admin, chỉ cần 1 lệnh lấy mảng này dựng cây Tree View giao diện cực nhanh, các Template chỉ việc map với ID của thư mục.
- **Decision (Dynamic Routing V2 & Safe Flush):** Nâng cấp `Ska_App_Router` để hỗ trợ bắt route động (`/slug/{id}`). Lệnh tử huyệt `flush_rewrite_rules()` TUYỆT ĐỐI chỉ được gọi khi lưu/cập nhật frontend_slug ở cài đặt Smart Object, nghiêm cấm chạy ở hook `init` gây nghẽn server.
- **Decision (Shadow FSE & Early Intercept):** Triển khai kiến trúc ký sinh bằng cách đặt Dispatcher tại hook `parse_request`. Đánh chặn URL từ rất sớm để nạp Context và ép WP render thẳng HTML của Ska, ngăn hệ thống đi tìm file `.php` vô ích. Đồng thời, Middleware kiểm tra quyền hạn (Role Checking) cũng nằm ngay tại đây để từ chối truy cập sớm, tránh tiêu tốn tài nguyên tải lõi WordPress.
- **Decision (Data Pro Auto-Indexing):** Bắt buộc Ska Data Pro phải tự động thêm `INDEX` cho mọi cột được khai báo là "Relation" (Khóa ngoại) khi build DB. Việc này quyết định sự sống còn của tốc độ Reverse Lookup (Rollup) trên hệ cơ sở dữ liệu phẳng, ngăn chặn Table Scan khi dữ liệu phình to.
- **Decision (Gutenberg UI Scope Restriction):** Cập nhật `portal-visibility.js` để giới hạn Panel cài đặt hiển thị (Visibility Scope) chỉ xuất hiện trong CPT `ska_theme_builder`, tránh làm "rác" UI.

---
## 2026-05-16 - 🔴 Pending: Kiến Trúc App Portal (Notion-like) & Data View
- **Decision (Data View & Portal Routing):** Mở rộng Ska Data Pro để quản lý App Portals (Cấp URL, Security Role) trực tiếp từ Smart Object. Thiết lập chế độ Data View (Read-only hoặc CRUD) đi kèm tính năng Relation & Rollup tự động render Sub-table.
- **Decision (Refine: Macro Pattern Injector thay vì Block Nguyên khối):** Thống nhất nút "Magic (Tự động tạo Template)" sẽ KHÔNG dùng một `Data View Block` đóng gói dạng hộp đen (Blackbox). Thay vào đó, nó sẽ kích hoạt **Macro Pattern Injector** của Theme Builder để tự động rải các khối Atomic (Ska Loop, Ska Text, Ska Button, Ska Modal) thành một bố cục hoàn chỉnh (Sidebar + Main Content). Bảo vệ tuyệt đối khả năng tinh chỉnh (Full Site Editing) cho End-User.
- **Decision (Event-Driven Data Flow):** Các thao tác Submit/Update từ form Data View ở Frontend sẽ ngầm bắn ra Hook (ví dụ: `ska_data_updated`). Ska Logic Engine sẽ bổ sung Trigger Node "Table Listener" để "hứng" sự kiện này và bơm `Record_Data` vào biến Input cho các luồng tự động hóa (Gửi Email, API...).

---
## 2026-05-13 - 🟢 Triển khai Dark Mode Engine (Ska Design System Phase 4.4)
- **Decision (Zero-Trash Dark Mode Toggle):** Quyết định không tạo block mới rác cho nút đổi Dark Mode. Thay vào đó, nâng cấp `ska-button` hiện có, bổ sung tuỳ chọn "Toggle Dark Mode" (`theme_toggle`) trong Inspector. Khi kích hoạt, block sẽ tự động chèn `@click.prevent="$store.skaTheme.toggle()"` để tương tác với Alpine Store.
- **Decision (Alpine.js State & Storage):** Thiết lập `Alpine.store('skaTheme')` quản lý trạng thái Dark Mode toàn cục (`isDark`), lưu trữ vào `localStorage` (`ska_dark_mode`) để duy trì trạng thái khi người dùng điều hướng qua các trang.
- **Decision (Anti-FOUC Injection):** Triển khai một inline script bằng PHP (`wp_head`, priority 0) để đọc `localStorage` và áp dụng class `dark` vào thẻ `<html>` ngay trước khi DOM bắt đầu render. Kỹ thuật này triệt tiêu hoàn toàn hiện tượng nháy màn hình trắng (Flash of Unstyled Content - FOUC).
- **Decision (JIT Compiler Integration):** Phê duyệt `Tailwind Compiler` hiện tại để xử lý modifier `dark:`, xuất ra CSS áp dụng scope chính xác. Đã vá lỗi (Hotfix) CSS Specificity & Dark Variant Resolution, đảm bảo trình biên dịch nhận diện biến cờ `$is_dark` khi duyệt Custom Brand Colors (Ví dụ: `dark:bg-primary` giờ sẽ tự trỏ đến biến `token_dark_color` màu đỏ `#ef4444` thay vì lấy nhầm mã màu Light mặc định). Tăng trọng số `html.dark body.ska-builder` để thắng trong CSS Cascade.
- **Decision (Global Store & Editor Dark Mode Preview):** Tích hợp Global Store (`$store.skaTheme`) vào lõi trình giả lập `SkapineJS` của Editor. Nâng cấp AST Evaluator để hỗ trợ biên dịch các hàm gọi (CallExpression) như `toggle()`. Block Editor giờ đây sẽ phản hồi nhạy bén và trực tiếp thao tác click vào Ska Button (Toggle Dark Mode) ở trạng thái Live Preview (tự động add class `.dark` vào thẻ root của Iframe Canvas), đảm bảo tính 1-1 giữa trải nghiệm biên tập và kết quả Frontend, đúng với triết lý Single Source Of Truth (Alpine.store).

## 2026-05-14 - 🟢 Thiết lập Kiến trúc App Dashboards & Auto-Generated CRUD (Phase 4.5 & 4.6)
- **Decision (App Portals Architecture):** Chốt sử dụng Shadow CPT (`ska_portal`) kết hợp Data Injection để lưu thiết kế giao diện vào bảng phẳng `ska_data_sys_portals` của Data Pro, chấm dứt việc dùng `wp_postmeta`. Sử dụng Virtual Wrapper của Theme Builder để xử lý Frontend Routing, cho phép cấu trúc Multi-page hoặc SPA.
- **Decision (Auto-Generated CRUD):** Hủy bỏ ý tưởng khối nguyên khối (Magic Block) để bảo vệ nguyên tắc Atomic. Triển khai theo mô hình **Macro Pattern Injector** (Máy rải khối nguyên tử). Hệ thống chỉ kéo thả hộ các khối (`Ska Loop`, `Ska Text`, `Ska Button`, `Ska Modal`), kết nối sẵn Event, rồi rút lui. Nocode Admin có toàn quyền Full Site Editing và tùy biến Logic Engine.

## 2026-05-14 - 🟢 Brainstorm: SkaWind JS (Editor JIT Compiler) cho Phase 6
- **Decision (Khai Sinh SkaWind JS):** Lên ý tưởng xây dựng một bộ biên dịch JIT bằng Vanilla JS siêu nhẹ (giống cách đã làm SkapineJS) chạy trực tiếp trên Trình duyệt của Editor để thay thế hoàn toàn Tailwind CDN trong tương lai. 
- **Decision (Giải Quyết Triệt Để Trộn Màu Dark Mode):** SkaWind JS sẽ sử dụng chung logic mảng màu (Array Swapping) với Ska JIT (PHP Backend). Việc này đảm bảo tính thống nhất 100% (Absolute Parity) giữa Editor và Frontend khi nội suy biến `dark:`, loại bỏ hoàn toàn sự phụ thuộc vào các trick CSS Specificity (`!important`) hay cấu hình phức tạp của Tailwind CDN. Ý tưởng này chính thức được đẩy vào Roadmap Phase 6 nhằm tối ưu hóa triệt để hiệu năng Builder.

## 2026-05-12 - 🟢 Mở rộng Design Tokens & Brand Identity
- **Decision (Brand Logo & Global Tokens):** Mở rộng hệ thống Design Tokens (Theme Options) cho phép lưu trữ và quản lý Brand Logo thông qua bảng phẳng `ska_data_sys_presets` (lưu dưới dạng `token_brand`). Tự động compile và xuất khẩu dữ liệu Brand ra file `/ska-data/tokens.json` làm Single Source of Truth (SSOT) cho Frontend.
- **Decision (SkaFX Global Dynamic Resolver - Ý tưởng tương lai):** Đưa ra định hướng kiến trúc cho SkaFX để xử lý **Dynamic Content Injection** (Nội suy nội dung động). Cụ thể, cho phép người dùng Nocode gán trực tiếp tham chiếu hệ thống ngay trong CodeMirror (Ví dụ: thay URL bức ảnh thành `[app.brand.logo]` hoặc `[app.sitemanager.preset.value.id=31]`). Trình biên dịch SkaFX sẽ tự động phân giải (resolve) token này ra URL hoặc giá trị cuối cùng ở Backend/Frontend mà không cần thiết lập vòng lặp hay payload phức tạp. Tính năng này được đưa vào Backlog cho Phase 4.

---
## 2026-05-12 - 🟢 Hoàn tất Visual Tailwind Browser (Phase 3) & Hybrid UX
- **Decision (Visual Browser Integration):** Tái khởi động Phase 3, chính thức triển khai `StylePopoverDrawer` đè lên giao diện `TailwindPanel`. Xây dựng cấu trúc UI trực quan phân loại Tokens (Theme Colors, Typography, v.v) với thanh search Vanilla JS Filter Array hiệu năng cao.
- **Decision (Dynamic Theme Integration):** Móc nối `skaDesignTokens.colors` (dữ liệu Data Pro) vào Visual Browser, tự động sinh các Utility Colors (`bg-primary`, `text-primary`) kèm theo giao diện Color Swatch (vòng tròn màu) chuyên nghiệp để tăng trải nghiệm thiết kế.
- **Decision (Fallback Color System):** Nếu người dùng không cấu hình Theme Options (`skaDesignTokens.colors` rỗng), hệ thống tự kích hoạt Fallback sang danh sách **Standard Colors** cơ bản của Tailwind (black, white, gray, red, blue...). Bảo vệ triệt để tính năng đổ màu cho người dùng độc lập.
- **Decision (Custom Arbitrary Class Injection):** Xóa bỏ điểm "break flow" trong thiết kế bằng cách tích hợp trực tiếp khả năng thêm Custom/Arbitrary Classes (Ví dụ: `h-48`, `w-[500px]`) ngay tại ô Search của Visual Browser, hỗ trợ chia tách nhiều class bằng Space và xử lý phím Enter native.
- **Decision (Overwrite vs Append Logic):** Chốt luồng dữ liệu 1 chiều từ Drawer về Input gốc. Quy tắc: **Append (Cộng dồn/Toggle)** đối với các Utility Classes thông thường, và **Overwrite (Ghi đè/Replace All)** đối với UI Presets (ví dụ Button Preset) để tránh rác class khi thao tác lặp lại. Tích hợp Snapshot state (Undo/Back) khôi phục UI block nếu user đổi ý muốn thoát.


---
## 2026-05-11 - 🟡 Khởi động Phase 3 & Hoàn tất Đồng bộ Design Tokens
- **Decision (Phase 3 Visual Tailwind Browser):** Thiết lập Implementation Plan xây dựng `ClassChipInput` và `StylePopoverDrawer` thay thế giao diện nhập class Tailwind cơ bản bằng ô text. Chốt phương án ưu tiên hiệu năng: không dùng mảng Fuse.js phức tạp hay tìm kiếm Server-side, thay vào đó xây dựng Vanilla JS Filter UI Array trực tiếp ở Frontend của Inspector. 
- **Decision (Phase 2 CamelCase Sync):** Xử lý dứt điểm mâu thuẫn Key Name (snake_case vs camelCase) giữa Frontend (Alpine.js State) và Backend (PHP Data Array). Toàn bộ Database của Theme Options và Tailwind JIT Compiler được nâng cấp để chấp nhận chuẩn `camelCase`, ngăn chặn rủi ro rớt dữ liệu (ví dụ: `containerWidth`). Hoàn tất 100% Phase 2 Design Tokens.

---
## 2026-05-11 - 🔴 Pivot Tối Thượng: Đập đi xây lại Design Tokens & Theme Options (3 Lớp)
- **Decision (Khai tử Theme Options Patching):** Từ bỏ cách tiếp cận "chắp vá" hiện tại (cố nhét Theme Options vào Ska System Dashboard và hardcode chuỗi Tailwind class dài thòng vào UI). Cách làm này phá vỡ SSOT (Single Source of Truth) và đi ngược lại triết lý Design System chuyên nghiệp (ví dụ như hệ thống Metric Flow).
- **Decision (Kiến Trúc 3 Lớp Design Engine):** Chốt phương án tái cấu trúc toàn diện theo 3 lớp:
  - **Lớp 1 - Design Tokens Registry:** Đóng vai trò Cốt lõi (The Core), nhận JSON/Markdown design chuẩn W3C và xuất ra biến `--color`, `--font` trên Root CSS. Không phụ thuộc Tailwind.
  - **Lớp 2 - Semantic Base Styling:** Giải quyết "Zero-config đẹp ngay" bằng cách cho phép JIT Compiler tự sinh các Global CSS rules (ví dụ: `h1 { @apply text-5xl font-bold text-primary; }`) để người dùng thả thẻ H1 là nhận style gốc, không cần bơm Utility Class.
  - **Lớp 3 - Visual Tailwind Browser:** Đập bỏ ô text input thuần của Tailwind Panel trong Inspector. Xây dựng một UI trực quan (chọn màu dạng Color Swatch, chọn spacing theo bảng Base Unit) và tự động gắn các class Tailwind nhưng xài giá trị từ Token (Ví dụ: `text-primary` thay vì `text-red-500`).

---
## 2026-05-10 - 🟢 Thiết lập Kiến Trúc Theme Options & Design Tokens (Phase 4.4)
- **Decision (Theme Options Dashboard Extension):** Thay vì tạo mới một trang Admin riêng biệt cồng kềnh, cấu hình Theme Options sẽ được tích hợp vào `Ska System Dashboard` thông qua hook `ska_system_dashboard_extensions`. Giao diện cấu hình (Modal) sử dụng Alpine.js để đảm bảo trải nghiệm lưu trữ mượt mà (0ms delay), không load lại trang.
- **Decision (Token Standardization):** Chốt quy chuẩn Token Hệ thống (Primary, Secondary, Surface, Text, Border). File Cache tĩnh `tokens.json` (do `Design_Tokens_API` xử lý) sẽ trở thành Nguồn sự thật duy nhất (Single Source of Truth) để cấp dữ liệu cho `Tailwind_Color_Registry`, đảm bảo Compiler nội suy mã màu CSS siêu tốc độ (N+0 Queries).

---
## 2026-05-10 - 🟢 Cập Nhật Cơ Chế JIT CSS Scanner & Zero CDN Policy
- **Decision (JIT Loop Scanner):** Quyết định nâng cấp cơ chế `Style_Manager::extract_block_classes` để đệ quy an toàn vào mảng `slots` của khối `ska-builder/loop`. Bổ sung điều kiện kiểm tra sự tồn tại của `$block['blockName']` để ngăn chặn lỗi cảnh báo PHP khi quét các phần tử thuần túy. Điều này đảm bảo mọi class Tailwind sinh ra từ giao diện vòng lặp đều được bắt giữ (capture) và biên dịch chính xác tại backend.
- **Decision (Global Archive Class Scanning):** Khước từ phương án kích hoạt Tailwind CDN trên trang chủ để bảo vệ điểm số Core Web Vitals (Zero CDN Policy). Thay vào đó, hàm `inject_tailwind_cdn()` được nâng cấp logic: Thay vì quét `$post_id = get_the_ID()`, hệ thống sẽ tự động quét (scan) toàn bộ danh sách bài viết trong biến `$wp_query->posts` khi người dùng truy cập trang chủ, Archive hoặc Search. Phương án này trả lại tốc độ tải tĩnh 100% (SSR) cho toàn bộ Frontend mà không phụ thuộc script ngoài.

---
## 2026-05-09 - 🟢 Hoàn thiện Cơ chế JIT CSS Injection & 404 Fallback (Ska Theme Builder Milestone 5)
- **Decision (Active Organism CSS Injection):** Để giải quyết lỗi "CSS không render trên giao diện" cho các phần tử độc lập (Header, Footer) do cơ chế JIT mặc định chỉ quét post content hiện tại, chúng tôi thiết lập cơ chế **Active Organism Injection**. Biến toàn cục `$ska_active_theme_organisms` lưu trữ ID của các Organism được `Virtual Wrapper` gài vào (Hook Priority 99). `Tailwind JIT Compiler` sau đó sẽ lấy danh sách ID này để tự động chèn thêm CSS của các block/organism ngầm này trước khi render HTML cuối.
- **Decision (404 Fallback Template):** Mở rộng tính năng điều hướng của `Virtual Wrapper` nhằm đánh chặn trạng thái `is_404()`. Triển khai File mẫu HTML tĩnh `404-template-demo.html` kèm Tailwind & Alpine.js, cho phép người dùng import linh hoạt vào giao diện mà không dính rác của Theme WP gốc, bảo vệ vòng đời thiết kế phẳng.
- **Decision (Theme Builder Stabilization):** Chính thức tuyên bố khép lại Phase 4.2 Ska Theme Builder. Cấu trúc Dual-Table kết hợp Iframe Editor Isolated và Virtual Wrapper (Frontend) đã chạy xuyên suốt và ổn định 100% trong bài kiểm tra E2E. Hệ thống sẵn sàng nhường chỗ cho Phase tiếp theo (Ska Molecules - UI Components).

---
## 2026-05-08 - 🟢 Triển khai Inline Dynamic Links (Ska Link Engine Milestone 3)
- **Decision (RichText Format Registration):** Áp dụng cơ chế đăng ký định dạng tùy chỉnh (`registerFormatType`) cho `RichText` để tích hợp `SkaLinkControl` trực tiếp vào thanh công cụ nội tuyến của Gutenberg. Quyết định này giúp giải quyết bài toán chèn link động vào từng từ/cụm từ bên trong một khối văn bản lớn (`ska-text`, `ska-list-item`) mà không làm gãy cấu trúc block.
- **Decision (Inline Link Backend Parser):** Triển khai hàm `resolve_inline_links` sử dụng Regular Expression để bóc tách và giải mã các thẻ `<a>` chứa thuộc tính `data-dynamic-source` và `data-dynamic-key` ngay trong phase render (SSR). Việc bóc tách diễn ra ở server giúp loại bỏ hoàn toàn mã JS dư thừa ở client, bảo toàn tiêu chuẩn Flat DOM và Zero N+1 Queries.
- **Decision (Data attributes mapping):** Gói gọn dữ liệu cấu hình link động vào các data attributes (`data-dynamic-source`, `data-dynamic-key`, `data-fallback`) trên thẻ `<a>` ở mode Editor để trình parser backend dễ dàng nhận diện và nội suy thành thẻ link hoàn chỉnh có chứa URL hoặc Mustache tags (`{{key}}`).

---
## 2026-05-08 - 🟢 Kiến Trúc Ska Link Engine (Dynamic Data Resolving)
- **Decision (Server-Side Link Resolving):** Quyết định loại bỏ giải pháp dùng `onclick` window.location.href (Client-side) cho các khối có chứa link để bảo vệ tuyệt đối chuẩn SEO (Search Engine Optimization) và khả năng mở link trong tab mới. Mọi thẻ mang URL (`<a>`) sẽ được xử lý ở Backend (Phase Render).
- **Decision (SkaLinkControl & Dynamic Source):** Ra mắt component React lõi `SkaLinkControl` tại Block Inspector. Cung cấp UX chọn Link tĩnh hoặc Link động. Link động hỗ trợ nguồn System (Home, Admin, Current Post) và nguồn Loop (Trích xuất từ bảng dữ liệu). 
- **Decision (Loop Hydration Compatibility):** Thay vì tự truy vấn SQL, thuộc tính Link Động nguồn Loop sẽ được tiện ích `Dynamic_Data` xuất ra dưới định dạng Mustache `{{key}}`. Sau đó, `Ska Loop Engine` sẽ sử dụng kỹ thuật preg_replace gốc để hydrate URL cùng lúc với các dữ liệu Text khác, triệt tiêu Over-engineering và bảo toàn Zero N+1 Queries.

---
## 2026-05-04 - 🟢 Chốt Kiến Trúc Ska Theme Builder (The Ultimate Hybrid)
- **Decision (Gutenberg as a Component Engine):** Quyết định loại bỏ hoàn toàn việc tích hợp sâu vào Full Site Editing (FSE) của WordPress do rủi ro "mất trí nhớ" khi tải dữ liệu, xung đột với hệ sinh thái WooCommerce và sự thiếu ổn định của FSE. Thay vào đó, thiết kế một **Ska Theme Panel** hoàn toàn độc lập (code bằng Alpine.js + Tailwind), đọc/ghi trực tiếp vào bảng phẳng `ska_data_sys_organisms`.
- **Decision (Smart Virtual Wrapper & Template Routing):** Triển khai kiến trúc **Smart Virtual Wrapper** để trung hòa giữa Website Builder và App Builder. Sử dụng hook `template_include` (Priority 99) để đánh chặn luồng render của WordPress. Trình Editor sẽ được chạy trong một Isolated Iframe toàn màn hình, cách ly với cơ chế lưu trữ của WP, đảm bảo dữ liệu Template (Header, Footer, Single...) được lưu dưới dạng Component trong Flat Tables.
- **Decision (Zero-Postmeta Policy Enforcement):** TUYỆT ĐỐI không tạo Custom Post Type (CPT) hay lưu trữ Template trong `wp_posts`/`wp_postmeta`. Mọi thao tác CRUD Template chỉ được phép giao tiếp thông qua REST API tới `ska_data_sys_organisms`.

---
## 2026-05-07 - 🟢 Phân tách Lộ trình Theme Builder (Dual-Table)
- **Decision (Project Management):** Quyết định tách riêng tiến trình phát triển của `Ska Theme Builder` ra file `project_manager_theme_builder.md` nhằm theo dõi sát sao 4 Milestones chính: Admin UI, Isolated Editor, Smart Virtual Wrapper, và Display Conditions.
- **Decision (Dual-Table Schema):** Khẳng định chiến lược lưu trữ kép. `ska_data_sys_theme_templates` sẽ giữ Metadata và cấu hình điều kiện hiển thị, trong khi `ska_data_sys_organisms` sẽ giữ payload nội dung thô (HTML/JSON).

---
## 2026-05-04 - 🟢 Tái định hình Kiến trúc: Theme Builder vs App Builder
- **Decision (Smart Virtual Wrapper Architecture):** Thống nhất lộ trình "Tách rời lũy tiến" (Progressive Decoupling) để giải quyết xung đột giữa Theme Builder (phụ thuộc WordPress header/footer) và App Builder (cần toàn quyền kiểm soát DOM). Hệ thống sẽ cung cấp chế độ lai: nếu dùng làm Website Builder, nó tiêm Hook vào Theme hiện tại; nếu dùng làm App Builder, nó sử dụng `virtual-wrapper.php` (Priority 99 trên `template_include`) để bypass Theme cũ hoàn toàn. Quyết định này bảo vệ khả năng tương thích của WooCommerce trong khi vẫn cho phép xây dựng Web App sạch sẽ.
- **Decision (Khước từ Output Buffering):** Quyết định không sử dụng kỹ thuật Output Buffering (`ob_start` trong `get_header`) để xóa CSS của Theme cũ do rủi ro Memory Leak, xung đột Caching/Minify và không tương thích với Block Themes (FSE).

---
## 2026-05-04 - 🟢 Tối ưu Hóa Hệ thống & Fix Lỗi Ska Loop Conditional Rendering
- **Decision (Tailwind JIT Class Merging):** Cập nhật cơ chế trích xuất Class trong `Style_Manager::scan_post_classes` để luôn **Merge** (kết hợp) cả 2 thuộc tính `className` và `tailwindClasses`. Vá lỗi JIT Compiler vô tình bỏ sót các lớp tiện ích (responsive `sm:`, pseudo `hover:`) từ Gutenberg khi hai thuộc tính cùng tồn tại.
- **Decision (Performance Cleanup):** Dọn dẹp (Cleanup) toàn bộ các lệnh in log debug (`error_log` và dump `debug_loop_render.html`) trong file `render.php` của Ska Loop nhằm tối ưu tốc độ render cho môi trường Production.
- **Decision (SkaFX Case-Insensitive Variable Resolution):** Giải quyết dứt điểm lỗi hệ thống không hiểu biến do khác biệt viết hoa/viết thường giữa cấu trúc Database (`Experience`) và cách User nhập trên UI (`$item.experience`). Cập nhật lõi `SkaFX_Evaluator` để toàn bộ quá trình ánh xạ key (kể cả dùng Dot Notation như `$item.experience` hay Key Root như `[experience]`) đều tự động rà soát **case-insensitive (không phân biệt chữ hoa/chữ thường)** làm fallback an toàn.
- **Decision (Ska Loop Truthy Condition Evaluation):** Cập nhật cơ chế đánh giá điều kiện trong vòng lặp `Ska Loop` (`render.php`). Chuyển từ kiểm tra bool khắt khe (`=== true`) sang kiểm tra "Truthy value" linh hoạt (Cho phép khớp nếu logic trả về `1`, `"1"`, `true` hoặc `"true"`). Sự thay đổi này bảo đảm Loop không bỏ sót dữ liệu khi user sử dụng các cột boolean trong MySQL (vốn lưu dưới dạng số nguyên `1/0`).

---
## 2026-05-04 - 🟢 Refactor Kiến Trúc: Đưa Layout Engine vào Ska Query Loop
- **Decision (Ska Loop as Structural Container):** Khai tử kiến trúc "Ghost Block" (phẳng hóa DOM bằng class `display: contents !important`) của Ska Query Loop. Do cơ chế bọc thẻ của Gutenberg (như `.block-editor-block-list__layout`) tạo ra quá nhiều thẻ div rác và cắt đứt luồng flex/grid từ cha xuống con, việc cố ép phẳng DOM gây lỗi dàn trang trong Editor dù chạy tốt trên Front-end. Quyết định: Chuyển đổi Ska Loop thành một **Structural Container** hoàn chỉnh.
- **Decision (Tailwind Panel Integration):** Tích hợp `<TailwindPanel>` trực tiếp vào khối Loop, cấp cho nó thuộc tính `tailwindClasses`. Từ nay người dùng Nocode có thể thiết lập layout (Flex/Grid, Gap) trực tiếp ngay trên Loop Block. Đồng thời, hàm `render.php` của Loop được cập nhật để bọc nội dung bằng một thẻ wrapper chuẩn xác, đảm bảo 100% đồng bộ DOM cấu trúc giữa Editor và Frontend, triệt tiêu lỗi layout vỡ khung.

---
## 2026-04-30 - 🟢 Bàn giao Phase 3 & Khởi động Phase 4 (Theme Builder)
- **Decision (Phase 3 Archive):** Đóng toàn bộ các task liên quan đến Logic Engine Core (Phase 3). Các tính năng như Webhook, Cron được chuyển sang Technical Backlog (Post-MVP). Hệ thống DAG hiện tại được coi là Stable và là nền tảng cho mọi automation.
- **Decision (Loop Block Priority):** Ưu tiên phát triển Ska Loop Block để hoàn thiện khả năng hiển thị dữ liệu từ Flat Tables. Tập trung vào UI Inspector trong React để người dùng cấu hình mà không cần gõ code.
- **Decision (Zero N+1 Enforcement):** Khẳng định lại nguyên tắc Zero N+1 Queries cho mọi block lặp dữ liệu. Sử dụng Bulk Loading và Hydration Engine đã xây dựng ở Backend.

---
## 2026-04-29 - 🟢 Xác thực thành công Cơ chế Smart Fallback & Hoàn tất E2E Testing MVP Primitives
- **Decision (Iterator / Loop Node Architecture):** Chốt phương án triển khai `[L3] Iterator Node` dưới dạng "Parent/Group Node" trên kiến trúc đồ thị DAG (React Flow) thay vì dạng nối tiếp truyền thống. Các Node con (nằm bên trong Iterator) sẽ được hệ thống gom lại, dùng thuật toán Topological Sorting để biên dịch tức thời (JIT Compilation) thành một chuỗi mảng tuyến tính và thực thi qua vòng lặp PHP thuần. Quyết định này giúp giải quyết dứt điểm bài toán Overhead đệ quy, không kích hoạt Circuit Breaker, và cung cấp một môi trường cục bộ cực nhanh với các biến ngữ cảnh `$item`, `$index`, `$first`, `$last` cho từng phần tử.
- **Decision (SkaFX Smart Prefix Stripping Verified):** Trong quá trình kiểm thử thực tế (End-to-End), người dùng xác nhận tính năng "Smart Fallback" cắt bỏ tiền tố `payload.` hoạt động hoàn hảo. Việc cho phép người dùng Nocode viết `{{ html_email.ma_html_content }}` thay vì `{{ payload.html_email.ma_html_content }}` đã giúp tối giản UX một cách triệt để mà vẫn giữ được tính kết nối dữ liệu an toàn từ kết quả của DB Query Node sang Render Template Node.
- **Decision (Logic Engine MVP Primitives Signed-off):** Luồng kiểm thử E2E tích hợp từ `Trigger` -> `Set Data` -> `DB Query` -> `Render Template` -> `Client Response` đã vượt qua 100% test cases cho cả chế độ System Organisms và Raw Variable. Cùng với việc bổ sung `Iterator Node`, toàn bộ Core Primitives của Ska Logic Engine MVP chính thức được đánh dấu Hoàn Thành và đi vào trạng thái Ổn định (Stable). Hệ sinh thái sẵn sàng chuyển trọng tâm sang **Phase 4: Giao diện Nocode & Ska Molecule**.
- **Decision (Iterator & SkaFX Stabilization):** Nâng cấp hệ thống để giải quyết các lỗi phát sinh trong quá trình thực tế: (1) **SkaFX Lexer $ Support**: Cho phép định danh bắt đầu bằng `$`. (2) **Ghost Parent Validation**: Ngăn lỗi gãy luồng do dữ liệu `parentId` rác từ React Flow UI. (3) **Auto-Sanitization**: Iterator tự động bóc tách ngoặc nhọn trong biểu thức nguồn mảng.
- **Decision (Node Hierarchy Architecture):** Chốt cấu trúc phân cấp 3 tầng cho Logic Engine: (1) **Primitive Nodes**: Các hạt cơ bản nhất. (2) **System Composite Nodes**: Các node tổng hợp sẵn (Macros) do hệ thống cung cấp (VD: Gửi Mail, Check-in). (3) **User Composite Nodes**: Node do người dùng tự đóng gói hoặc import từ Marketplace.
- **Decision (AI-Ready Blueprint Integration):** Chuẩn hóa định dạng JSON Import/Export cho toàn bộ Workflow và Preset Nodes. Mục tiêu: Cho phép AI (Ska AI Architect) sinh mã JSON cấu trúc Node tự động, biến Logic Engine thành một "Execution Runtime" cho các chỉ thị từ AI.
- **Decision (Phase Transition):** Chính thức đóng Phase 3 (Logic Engine Core) và chuyển sang Phase 4 tập trung vào **Theme Builder (Ska Loop Block)** để hoàn thiện khả năng hiển thị dữ liệu động.

## 2026-04-28 - 🟢 Triển khai Render Template Node [R1]
- **Decision (Render Template Node Decoupled Architecture):** Quyết định triển khai khối "Render Template" như một cỗ máy biên dịch mini (Pure Interpolation Primitive), sử dụng `preg_replace_callback` kết hợp với `SkaFX_Engine` để quét và nội suy các biến `{{ ... }}` vào bên trong khung HTML. Nhằm tách bạch khả năng truy vấn dữ liệu ra khỏi node render, Node đã được nâng cấp hỗ trợ 2 nguồn (Source Type): Lấy mã HTML mẫu từ bảng `ska_data_sys_organisms` dựa theo `organism_id`, hoặc lấy toàn bộ cấu trúc HTML được truyền vào dạng chuỗi/biến qua thuộc tính `raw_template` (ví dụ từ DB Query Node).
- **Decision (Template Output Variable):** Kết quả HTML sau khi được nội suy sẽ được lưu thẳng vào Payload thông qua một biến cấu hình (mặc định là `payload.rendered_template`). Dữ liệu này sẽ làm mồi để cấp cho các Node tiếp theo, ví dụ như đưa vào `ClientResponseNode` để hiển thị Modal chứa dữ liệu động, hoặc cấu thành nội dung thư gửi tự động qua email.

## 2026-04-28 - 🟢 Mở Rộng SkaFX Engine & Chốt DB Query Node
- **Decision (SkaFX Array Properties & Built-in functions):** Quyết định mở rộng 문법 (Syntax) của SkaFX Engine để hỗ trợ trực tiếp các tính năng thao tác mảng. Bổ sung khả năng đọc thuộc tính độ dài `.length` (Ví dụ: `[array.length]`) và hàm built-in `LIST_COL(array, 'col_name', 'separator')` giúp người dùng Nocode có thể đếm số lượng bản ghi hoặc xuất chuỗi danh sách một cách dễ dàng mà không cần dùng Node "Code" hay vòng lặp phức tạp.
- **Decision (SkaFX Smart Fallback Prefix):** Bổ sung thuật toán thông minh tự động dọn dẹp (sanitize) tiền tố `payload.` nếu người dùng Nocode nhập nhầm vào ô biến (Ví dụ: nhập `payload.ho_ten` thay vì `ho_ten`). Giúp giảm thiểu lỗi gãy luồng Logic do sai sót đánh máy cơ bản, nâng cao tính Human-Centric cho Logic Engine.
- **Decision (DB Query Node Logging):** Tích hợp tự động in truy vấn SQL (`error_log`) vào `Ska_Logic_DB_Query` để phục vụ Debugging. Node DB Query chính thức hoàn thiện, sẵn sàng đóng vai trò Read/Fetch dữ liệu động từ Database trong luồng tự động hóa.

## 2026-04-28 - 🟢 Brainstorm: Phân rã Trigger Frontend (Alpine) & Backend (Logic Engine)
- **Decision (Frontend vs Backend Trigger Boundary):** Thống nhất và phân rạch ròi ranh giới xử lý sự kiện:
  - **Sự kiện Giao diện (UI Interactions):** Các tác vụ như mở Popup sau 10s, cuộn chuột, bật tắt Modal BẮT BUỘC xử lý bằng **Alpine.js** (Ska Design Engine) tại trình duyệt (Frontend) để đảm bảo tốc độ Zero-latency và không làm quá tải Server. Tuyệt đối không dùng Logic Engine cho mục đích UI thuần túy.
  - **Sự kiện Tự động hóa (Business Logic):** Các tác vụ như Lưu Form, Thanh toán, Gửi Email sẽ được xử lý độc quyền bởi **Logic Engine** (Backend) để đảm bảo bảo mật.
- **Decision (Webhook vs System Hook):** Khẳng định vai trò của `[T1] Trigger Node`. Webhook (URL) dùng để nhận dữ liệu từ hệ thống bên ngoài (Stripe, Facebook) qua HTTP. System Hook (Internal Event) dùng cho các luồng xử lý siêu tốc trong cùng máy chủ (RAM), cấm dùng Webhook cho sự kiện nội bộ để tránh Network Overhead.
- **Decision (Template Settings / Display Rules):** Khẳng định Popup toàn cục (Global Popups) không được hardcode vào Atomic Blocks. Chúng được quản lý dưới dạng **Smart Object (Template)**. Các "Điều kiện Kích Hoạt" (Delay, Scroll, Scope) sẽ được cấu hình bằng giao diện Nocode (Settings Panel) ở cấp độ Tài liệu. Compiler sẽ tự sinh mã Alpine.js cấu hình và nạp vào Global Listener. Lùi tiến độ tính năng này xuống Phase Theme Builder.

## 2026-04-28 - 🟢 Brainstorm: Ska Scripts Library & Global Code Module
- **Decision (Ska Scripts Library):** Thống nhất kiến trúc xây dựng Thư viện Code tập trung (Ska Scripts Library) dựa trên nền tảng Smart Object `app-site` hiện có. Cho phép người dùng lưu trữ các đoạn mã JS/CSS (như Alpine.store, Chart.js CDN, GA4) và cấu hình "Vị trí nạp" (Header/Footer/Inline) cùng "Phạm vi nạp" (Global/On-demand).
- **Decision (Khối `ska-code` & Deduplication):** Thay thế khối Custom HTML thô sơ bằng khối `ska-code` chuyên dụng. Khối này hỗ trợ viết code nội tuyến (Inline) hoặc gọi code từ Thư Viện (Library Injection). Lõi hệ thống sẽ tự động gom mã (Harvesting) và chống lặp (Deduplication) để tối ưu tốc độ tải trang (Core Web Vitals).
- **Decision (Post-MVP Deferral):** Chốt đưa tính năng Ska Scripts Library và khối `ska-code` vào lộ trình Post-MVP, tập trung ưu tiên hoàn thành lõi Logic Engine DAG MVP trước. Dùng giải pháp SkaFX nội suy tạm thời cho đến khi tính năng này ra mắt.

## 2026-04-28 - 🟢 Hợp Nhất & Tinh Gọn: 9 Core Primitives (Ska Logic Engine)
- **Decision (Gộp Trigger Nodes):** Hủy bỏ `[T3] Action Click` và sáp nhập hoàn toàn vào `[T1] Trigger Node`. Chuyển đổi Trigger Node thành điểm vào (Entry Point) duy nhất với thuộc tính `Trigger Type` (Logic Trigger, Webhook, Cron). Quyết định này giúp bảo vệ tính "nguyên tử" (Primitive) tối đa, giảm thiểu phân mảnh Node.
- **Decision (Bổ sung DB Query Node):** Chốt đưa `[D2] DB Query Node` vào Roadmap MVP (Ưu tiên P2). Node này chuyên trị việc đọc (Fetch), lọc (Where), và giới hạn (Limit) dữ liệu từ CSDL bảng phẳng (`ska_data_*`), bổ trợ sức mạnh truy vấn còn thiếu của `DB Action Node` (vốn chỉ chuyên Mutate). 
- **Decision (Tài liệu Kiến trúc Chuẩn):** Phát hành tài liệu đặc tả `primitive-nodes.md` làm Source of Truth duy nhất cho toàn bộ 9 Hạt cơ bản, loại bỏ hoàn toàn các ý tưởng về Compound/Composite Node trong giai đoạn MVP hiện tại.

## 2026-04-28 - 🟢 Tối ưu Hóa Global State & Sự kiện cục bộ (Alpine.store)
- **Decision (Alpine.store Local State):** Áp dụng kiến trúc Single Source Of Truth với `Alpine.store` để thiết kế các nút "Local Pop-up" hoặc "Toggle UI" (không gọi API Backend). Tách biệt hoàn toàn tính năng UI Response khỏi luồng AJAX Logic Engine để đạt độ trễ 0ms (Zero-Latency).
- **Decision (Bắt buộc Khai báo x-data):** Rút kinh nghiệm quan trọng từ phiên bản Alpine.js v3: Bất kể một thẻ HTML nào muốn gọi `$store` thông qua `@click` hay `x-show`, bản thân nó hoặc thẻ cha trực tiếp BẮT BUỘC phải được khai báo `x-data` (có thể để trống). Đây là rule cứng của Alpine v3 để định danh một Component Node và bắt đầu lắng nghe directives.

## 2026-04-27 - 🟢 Nâng cấp Ska Button: Hỗ trợ AJAX Trigger & Local UI Event
- **Decision (Ska Button Action Types):** Tái cấu trúc thuộc tính `actionType` của khối Ska Button, hợp nhất tính năng gọi API và hiển thị Popup. Phân rã rành mạch thành 2 chế độ: `Trigger Workflow (AJAX)` và `Trigger Local UI Event`.
- **Decision (Inline AlpineJS x-data):** Thay vì nạp một thư viện JS cồng kềnh để lắng nghe click cho Ska Button, áp dụng trực tiếp `x-data` và `@click.prevent` của AlpineJS để bắn Fetch API (`POST /wp-json/ska-logic/v1/submit`). Logic này xử lý cả trạng thái `loading` và tự động dispatch các lệnh UI (như Mở Modal, Hiện Toast) trả về từ Backend thông qua `window.dispatchEvent(new CustomEvent('ska-client-response'))`. Đối với Local UI Event, nút sẽ ném event trực tiếp mà không tốn chi phí gọi server.

## 2026-04-27 - 🟢 Hoàn thành Frontend Builder Settings cho Client Response
- **Decision (Client Response Settings UI):** Cập nhật `SettingsPanel.jsx` để thêm cấu hình thuộc tính nâng cao cho `ClientResponseNode` bao gồm Loại Phản hồi (Toast, Redirect, Modal, Fire Event) và Message/URL tương ứng, hỗ trợ nhập tĩnh hoặc SkaFX. UI giờ có thể biên dịch ra payload đúng chuẩn JSON.
- **Decision (End-to-End Event Bus):** Chốt luồng sự kiện truyền từ Frontend (nút bấm độc lập `ska-action`) -> Logic Engine (Node xử lý dữ liệu và bắn sự kiện `_ska_events` xuống payload) -> Trình duyệt bắt được luồng `toast` và sinh thông báo thông qua Window Event Bus của `ska-core.js`.
- **Decision (Merge Trigger Popup):** Hợp nhất tùy chọn `Trigger Popup` vào `Trigger Logic Workflow` trong khối `ska-button`. Nhờ có `ClientResponseNode` xử lý mở Modal từ phía Logic Engine, việc cấu hình popup trực tiếp trên nút bấm bị gỡ bỏ để đảm bảo luồng Logic Event Bus xuyên suốt và decoupled hoàn toàn.

## 2026-04-27 - 🔴 Pivot Tối Thượng: Kiến Trúc 7 Hạt Cơ Bản (True Primitives)
- **Decision (Khai tử các Node Lai / Composite MVP):** Chốt phương án hủy bỏ việc xây dựng các Node chức năng lai (như Node Gửi Mail, Node Tạo PDF, Node Hiện Pop-up) trong giai đoạn MVP. Dựa trên triết lý tách nhỏ tuyệt đối (Atomic), các chức năng này thực chất sẽ được ghép từ các Composite Nodes trong tương lai.
- **Decision (7 True Primitives):** Khẳng định Lõi hệ thống Logic Engine MVP chỉ được phép duy trì đúng 7 Node nguyên thủy: (1) `Trigger`, (2) `Set Payload`, (3) `If/Else/Loop`, (4) `DB Action`, (5) `Raw HTTP Request`, (6) `Render Template` (Móc Smart Object & đắp dữ liệu HTML), và (7) `Client Response` (Trả tín hiệu JSON về trình duyệt để hiển thị Modal/Toast/Redirect).
- **Decision (Render Template & Client Response):** Bổ sung 2 node mảnh ghép cuối cùng vào lộ trình. Tích hợp Hydration Engine vào `Render Template` để tạo mâm xôi (HTML) phục vụ Email/Pop-up. Quy hoạch gộp 3 node (Client Modal/Toast/Redirect) thành một node duy nhất là `Client Response`, làm cổng giao tiếp độc quyền với Alpine.js ở Frontend.

## 2026-04-27 - 🟢 Kiến Trúc Phân rã Client Action Nodes (Phản hồi UI)
- **Decision (Bảo Vệ Tính Primitive của UI Response):** Thay vì tạo một Node "God Object" khổng lồ gánh mọi thao tác trình duyệt (Chuyển trang, Pop-up, Thông báo, Update Giỏ hàng) gây phá vỡ triệt để kiến trúc Primitive, chúng tôi quyết định băm nhỏ tính năng "UI Response" thành nhóm 3 Primitive Nodes độc lập: `[C1] Client Redirect` (Chuyển trang), `[C2] Client Notification` (Thông báo Toast/Alert), và `[C3] Client State` (Giao tiếp với Alpine.store để bật tắt Lightbox, Cập nhật giỏ hàng). Cơ chế này giúp giữ nguyên vòng đời Đơn nhiệm (Single Responsibility) của hệ thống DAG, ngăn chặn Spaghetti UI ở Backend.

## 2026-04-27 - 🟢 Hoàn thành Raw HTTP Request (GET) & Action Click Trigger
- **Decision (Raw HTTP Request Node - GET Pipeline):** Chốt kiến trúc và hoàn thành luồng gọi API ngoại vi thông qua `Ska_Logic_Http_Request`. HTTP Node hiện đã tương thích hoàn toàn với DAG pipeline, nhận `url`, `method`, `headers`, và `query_params`, hỗ trợ Nội suy dữ liệu `{{ ... }}` động. Cho phép nhận phản hồi (Response JSON) và chèn ngược vào mảng `$payload` để Node tiếp theo (như DB Action Node) tiến hành ghi vào CSDL an toàn.
- **Decision (Unified Action Click Listener):** Độc lập hóa Trigger: Nút bấm (Ska Button) không còn bắt buộc phải nằm trong Form để gửi dữ liệu. Áp dụng cơ chế Global Event Listener trên class `ska-action-[workflow_id]` tại `ska-core.js`. Khi Button mang class này được bấm, nó sẽ gửi tín hiệu kích hoạt luồng Workflow độc lập (Async) xuống Backend (Endpoint: `/ska-logic/v1/submit`), mở đường cho kiến trúc UI "Smart Lightbox/Popup" tự kích hoạt dữ liệu trong tương lai.

## 2026-04-26 - 🟢 Debug Hoàn tất Luồng Dữ liệu Ska Logic Engine & Giao diện Node
- **Decision (SkaFX Smart Fallback cho Chuỗi Tĩnh):** Giải quyết triệt để lỗi người dùng Nocode nhập "chuỗi tĩnh" (không có nháy kép, VD: `khách lẻ`) vào DB Action Node khiến hệ thống tưởng nhầm là tên biến và đánh giá ra `null` (hoặc lỗi cú pháp). Quyết định: Cập nhật `SkaFX_Evaluator` để trả về cờ báo lỗi chính xác, và cập nhật luồng Mapping của Node DB Action bằng cơ chế **Smart Fallback** (Fall back to string). Nếu hệ thống báo lỗi không phân giải được biểu thức, chuỗi đó sẽ được hiểu là văn bản thuần túy và tự động Parse tham số bên trong ngoặc vuông (VD: `[hoten]`) bằng Regular Expression, đảm bảo luồng Logic không bao giờ chèn dữ liệu rỗng (`NULL`) một cách vô lý vào DB.

- **Decision (SkaFX Evaluator & Condition Syntax):** Hướng dẫn người dùng Nocode hiểu đúng cú pháp của `If/Else Node`. Biểu thức điều kiện cần trả về boolean (VD: `[tuoi] > 18`) thay vì phép gán hay so sánh với biến rỗng (`[tuoi] > 18 == active`). Đã xác minh `Ska_Workflow_Runner` định tuyến nhánh `true`/`false` chính xác khi biểu thức hợp lệ.
- **Decision (React Flow UI State Preservation):** Sửa lỗi giao diện `SetDataNode` trên Canvas ghi đè (overwrite) description của người dùng bằng generic assignments (Sets:...). Quyết định tái cấu trúc `SetDataNode.jsx` để đẩy nội dung assignments xuống `children` của `BaseNode`, giữ nguyên vẹn description tùy chỉnh và hiển thị đồng thời cả 2 khối thông tin gọn gàng.
- **Decision (Workflow Execution Validation):** Chốt chặn và xác nhận toàn bộ Data Pipeline từ `Trigger (Form)` -> `Set Data (SkaFX)` -> `Condition (If/Else)` -> `DB Action (Insert)` hoạt động hoàn hảo trong mô hình DAG mà không bị gãy chuỗi tham chiếu `$payload`.

## 2026-04-24 - 🟢 Tối ưu Hóa Giao diện Quản trị & Dashboard Ecosystem
- **Decision (Dynamic Workflow Submenu):** Quyết định đăng ký tự động các luồng workflow (Ska Logic Engine) thành các menu con ở thanh quản trị WordPress. Thay vì người dùng phải vào giao diện chính rồi ấn chọn luồng, giờ họ có thể truy cập (1-click) ngay từ sidebar, giúp giảm thiểu độ trễ thao tác UX.
- **Decision (Unified Module Card UI):** Áp dụng kiến trúc thiết kế thẻ `.module-card` cao cấp dùng chung cho cả Hệ sinh thái Ska thông qua lớp System Framework. Giao diện Dashboard được nâng cấp các hiệu ứng hover, shadow, và status indicators giúp mang lại trải nghiệm Nocode đồng bộ, chuyên nghiệp cho mọi Plugins.
- **Decision (Builder UI Stability):** Vá lỗi tràn giao diện (Overflow) khi render Data Picker với quá nhiều trường dữ liệu tại `SettingsPanel` bằng CSS `flex-shrink-0` và word-wrap. Tích hợp Keyboard listener bắt phím `ESC` để đóng modal giúp cải thiện thao tác điều hướng cho người dùng.
- **Decision (Ska_Logic_Set_Data Compatibility):** Xử lý dứt điểm cảnh báo undefined index trong lưu trữ backend và khắc phục xung đột chữ ký (signature) phương thức `execute()` của `class-ska-logic-set-data.php` để tương thích chuẩn `Ska_Logic_Node`, đảm bảo vòng đời Node Pipeline chạy ổn định, không ném PHP Fatal error.
## 2026-04-24 - 🟢 Hoàn thành Primitive Node: Set Data & Gán Biến (Ska Logic Engine)
- **Decision (Data Mutability & The Payload Container):** Khẳng định cơ chế "Mutate by Reference" ở mức Runner thông qua việc trả về mảng. Node `Set Data` trực tiếp sửa đổi mảng `$payload`, các block phía sau luôn có thể gọi `{{ trigger.key }}` mà không cần phải ánh xạ 1:1 các cột của Form hay phải dùng Node Map cồng kềnh.
- **Decision (SkaFX Expression cho Data Hydration):** Áp dụng engine SkaFX vào việc tính toán và truyền biến. Cho phép gán các biểu thức phức tạp, chuỗi xen kẽ biến `Tiền thu: {{ trigger.tien }}` hoặc gọi biến có tên tiếng Việt khoảng trắng `{{ trigger['số điện thoại'] }}` vào dữ liệu của Payload nhằm đảm bảo Nocode Natively.
- **Decision (Gỡ bỏ tham chiếu & trong Interface):** Gỡ bỏ ký tự `&` trong signature hàm `execute(&$payload)` của `class-ska-logic-set-data.php` để tương thích hoàn toàn với interface `Ska_Logic_Node`, ngăn chặn PHP Fatal Error Declaration.

## 2026-04-24 - 🟢 Tối ưu Hóa Kiến Trúc: Loop JIT Compilation & Logic Molecules
- **Decision (JIT Compilation cho Loop):** Chốt kiến trúc "Biên dịch tức thời" cho các vòng lặp. Thay vì thông dịch (interpret) và lưu State 10,000 lần cho 1000 items, Logic Engine sẽ quét (Compile) các Node con bên trong Node Vòng Lặp thành một Hàm ẩn danh (PHP Closure) hoặc Pipeline tuyến tính. Hàm này sẽ được gọi trực tiếp trên RAM cho mỗi item, triệt tiêu hoàn toàn Overhead của State Transitions.
- **Decision (Logic Molecules / Compound Nodes):** Chính thức định hình lớp Node trung gian (Compound Nodes) nằm giữa Primitive (Nguyên tử) và Composite (Đại phân tử). Các Compound Nodes (như Switch Router, DB Upsert, Array Aggregator) sẽ bọc nhiều thao tác Primitive phức tạp lại thành 1 giao diện duy nhất, cứu rỗi UX thiết kế No-code khỏi tình trạng Spaghetti.
- **Decision (Composite Node Storage & Global Edit):** Đồng thuận tái chế 100% kiến trúc của khối `Ska Organisms` (Ska Data Pro) sang cho Composite Nodes. Lưu JSON đồ thị của Macro xuống bảng phẳng `ska_data_sys_logic_macros`. Khối trên Canvas chỉ giữ `refId`. Tích hợp tính năng "Sửa Bản Gốc" (Edit Source) thông qua Iframe/Modal cách ly và đồng bộ Real-time Cache qua `postMessage`.

## 2026-04-24 - 🟢 Logic Engine UI: React Flow Provider & Inspector Architecture
- **Decision (ReactFlowProvider & Sidebar DnD):** Áp dụng kiến trúc `ReactFlowProvider` để bọc toàn cục ứng dụng Node Builder, cho phép xử lý Drag & Drop (kéo thả) các node từ Sidebar vào Canvas bằng API `screenToFlowPosition` mượt mà và chuẩn xác.
- **Decision (Settings Panel & 2-Way Binding):** Tách biệt Sidebar (chứa danh sách Nodes) và Settings Panel (Inspector cấu hình Node). Inspector tự động cập nhật State và re-render dữ liệu lên Node Canvas theo thời gian thực (Real-time 2-way binding), đồng thời tự động lưu trữ cấu trúc mảng JSON xuống trường ẩn (hidden input) của WordPress để lưu trữ.
- **Decision (Generic BaseNode Architecture):** Quy hoạch thiết kế chuẩn hóa cho 10 Atomic Nodes bằng cách tạo ra một `BaseNode` bọc ngoài. Khối lượng code lặp lại (hiển thị Handles/Ports ngõ ra-vào, icon, tên node) được gom chung, giúp quá trình triển khai các logic node cụ thể (If/Else, DB Action) nhanh chóng và triệt tiêu lỗi UI.

## 2026-04-24 - 🔴 Pivot Architecture: Primitive & Composite Nodes
- **Decision (Primitive + Composite Architecture):** Thay đổi định hướng xây dựng hệ sinh thái Node từ "các node đóng hộp to bản" sang mô hình Core Primitives (Trigger, Logic, Data, Protocol, Ska Native). Việc này loại bỏ hoàn toàn gánh nặng maintain API bên thứ 3 và tăng sức mạnh linh hoạt. Post-MVP sẽ triển khai "Composite Nodes" cho phép đóng gói các Primitive lại thành Node macro dùng chung (giải quyết learning curve cho pure no-code user).
- **Decision (Progressive Implementation):** MVP (Phase 4.2) sẽ tập trung release Primitive Nodes trước để đảm bảo tiến độ. Việc lưu/gọi Composite Nodes bị đẩy lùi sang bản cập nhật sau.
- **Decision (Circuit Breaker & State Pruning):** Nhằm giải quyết Performance Overhead khi chạy các primitive nodes qua vòng lặp, hệ thống lõi áp dụng State Pruning (dọn dẹp dữ liệu bước trung gian không xài tới) và Circuit Breaker (ngắt mạch khi số bước vượt trần hoặc lặp vô hạn). Hỗ trợ thêm Async Batch processing.
- **Decision (N+1 Query Prevention - Data Vector):** Action nodes (đặc biệt là DB Action) được thiết kế theo dạng Data Vector (Array-Aware), có khả năng nhận input là Array để thực thi Bulk Query (gộp SQL), qua đó phá giải triệt để nạn N+1 Queries khi ở trong Loop.

## 2026-04-24 - 🔴 Pivot Architecture: Logic Engine Automation Platform
- **Decision (N8N-like Canvas UI & DAG):** Chốt phương án đập bỏ giao diện Linear Node (tuyến tính) hiện tại. Nâng cấp Ska Logic Engine lên mô hình 2D Canvas Graph (React Flow) với cấu trúc lưu trữ đồ thị phân nhánh (Directed Acyclic Graph - DAG) để diễn đạt các luồng Logic phức tạp.
- **Decision (Trigger Nodes & API Expansion):** Mọi workflow giờ đây bắt buộc bắt đầu bằng một **Trigger Node** (Form Submit, Webhook In, Cron Schedule). Điều này mở đường cho việc gọi và nhận API bên ngoài, biến Ska Logic Engine thành một Nền tảng Automation thực thụ.
- **Decision (Multiple Outputs & Error Handling):** Các Node (đặc biệt là API Node) sẽ có đa ngõ ra (Cổng Success 🟢 và Cổng Error 🔴), cho phép xây dựng luồng Try/Catch rẽ nhánh không bao giờ Crash.
- **Decision (Async Execution - Background Process):** Thêm cờ "Chạy nền" (Async Flag) ở các điểm nối (Edges). Các tác vụ nặng (Gọi API, Gửi hàng loạt Email) sẽ được chuyển xuống hàng đợi Action Scheduler/WP-Cron để không làm tắc nghẽn (Block) giao diện Frontend của End-user.
- **Decision (AI JSON Blueprint Import):** Xác lập chuẩn lưu trữ Workflow bằng JSON Blueprint (Node & Edges). Tính năng này nhắm mục đích lấy AI làm trung tâm: User có thể nhờ AI viết 100% logic workflow và chỉ cần 1-click Import thay vì tự kéo thả thủ công.

## 2026-04-24 - 🔴 Định Hướng Rebuild Logic Engine & Hoãn Loop Block
- **Decision (Logic Engine Workflow UX Rebuild):** Tạm hoãn toàn bộ tiến độ của Ska Query Loop Block để dồn toàn lực thiết kế lại giao diện của Logic Engine Nodes (cụ thể là các node Insert/Update Record). Quyết định này nhằm giải quyết triệt để rào cản Low-code (bắt người dùng tự gõ tay tên cột cơ sở dữ liệu và tên biến payload). Giải pháp thay thế sẽ bao gồm: Schema-driven UI tự tải danh sách cột từ DB, hệ thống Data Picker (chọn biến trực quan qua dropdown), và thuật toán Auto-mapping.
- **Decision (Project Management Separation):** Bóc tách Loop Block ra một project manager file riêng (`project_manager_ska_loop_block.md`) và thiết lập trạng thái `Paused`. Đồng thời, khởi tạo `project_manager_logic_engine_rebuild.md` để dẫn dắt lộ trình 4 bước đập đi xây lại Logic Node UI trong phiên làm việc tiếp theo.

## 2026-04-23 - 🔴 Khủng hoảng UX Nocode Form & Pivot Kế hoạch
- **Decision (Form Builder UX Pivot):** Thừa nhận thất bại trong trải nghiệm người dùng (UX) của giải pháp kết nối Form thủ công. Việc bắt buộc người dùng Nocode tự gõ các attribute `x-data="skaForm()"`, `fields.tên_trường`, `status.submitting` thông qua Inspector Gutenberg là quá rủi ro và bất khả thi cho non-coder. Quyết định: Phải có giải pháp "Abstraction Layer" (Ví dụ: Form Builder UI tự động map thẻ HTML với Workflow) để giấu hoàn toàn Alpine.js đi. Tạm ngưng để suy nghĩ giải pháp ở phiên sau.

## 2026-04-23 - 🟢 Khôi phục Alpine Form Integration & System Initialization
- **Decision (Script Dependency Injection):** Bổ sung dependency `'ska-frontend'` vào hook đăng ký `ska-alpine` script (`wp_register_script` trong `blocks/init.php`). Quyết định này giải quyết dứt điểm rủi ro Race Condition khiến thư viện Alpine.js khởi chạy vòng lặp DOM Tree (quét `x-data`) trước khi Controller `skaForm` (trong `ska-frontend.js`) được định nghĩa, triệt tiêu lỗi `doctorForm is not defined`.
- **Decision (State Synchronization):** Quyết định đồng bộ tuyệt đối Cấu trúc State của Frontend HTML (Template `doctor_form.html` và page `doctor-form` trong CSDL) với khung thiết kế chuẩn của Logic Engine (`skaForm()`). Tiến hành thay thế mảng State cũ (`formData.*`, `isSubmitting`) sang mô hình đóng gói `fields.*` và `status.*` để tận dụng trọn vẹn sức mạnh Auto-Validation và Data Handling của Backend Pipeline.

## 2026-04-23 - 🟢 React Inspector cho Ska Select & Zero N+1 Auto-Generation
- **Decision (Dynamic Data Binding Inspector):** Bổ sung UI bật/tắt (Toggle) "Nguồn dữ liệu động" tại React Inspector của khối `Ska Select`. Gỡ bỏ check khắt khe `__table_info` giúp tất cả các bảng (kể cả bảng do user tạo) đều hiển thị. Áp dụng Protective UX bằng cách chỉ cho phép người dùng chọn các cột có dữ liệu Option (`select/radio/checkbox`), ngăn chặn rủi ro lỗi giao diện khi gán nhầm trường dữ liệu Text/Number.
- **Decision (Template Auto-Generation):** Chấp nhận tự động sinh chuỗi HTML nội suy (`<option value="{{value}}">{{label}}</option>`) nếu block Select đang gọi vòng lặp từ React UI mà không cung cấp template body. Quyết định này giúp làm gọn UI thiết lập (Zero config), nhưng vẫn đáp ứng được tính toàn vẹn của cấu trúc Zero N+1 Queries (Foreach) mà `Ska Logic Engine` yêu cầu tại Frontend.

## 2026-04-22 - 🟢 Ska Loop Block (Backend Core) & Hospital Template
- **Decision (Zero N+1 Query Architecture):** Triển khai cơ chế Backend Rendering cho `Ska Loop Block`. Quyết định sử dụng `Organisms_API::get_bulk_html` để load hàng loạt (Bulk Load) HTML của các Symbol Template trước khi vòng lặp bắt đầu. Cấm tuyệt đối việc Query SQL riêng lẻ (`get_table_data`) bên trong vòng `foreach`.
- **Decision (Hydration Engine & Mustache Syntax):** Không sử dụng cơ chế nối chuỗi HTML chậm chạp. Áp dụng kỹ thuật Hydration siêu tốc bằng hàm `preg_replace_callback` kết hợp cú pháp Mustache (`{{key}}`) để đắp dữ liệu từ mảng phẳng (Flat Tables) vào thẳng cấu trúc HTML của Organism.
- **Decision (SkaFX Lexer Upgrade cho Loop Variables):** Chấp nhận bổ sung tiền tố biến hệ thống (`$`) vào bộ từ vựng SkaFX (Ska Logic Engine). Từ nay SkaFX có thể xử lý các biến ngữ cảnh nội bộ của vòng lặp như `$index`, `$first`, `$last` để đánh giá điều kiện hiển thị hiển thị (Condition Matching) ở các Slot của Loop Block.

## 2026-04-22 - 🟢 Global Edit (Shadow CPT) & Môi trường Cách ly
- **Decision (Shadow CPT Architecture):** Quyết định triển khai tính năng "Sửa Bản Gốc" (Global Edit) cho Ska Organisms dựa trên kỹ thuật **Shadow CPT (`ska_organism_draft`) kết hợp Full-screen Iframe Modal**. Kiến trúc này cho phép tận dụng trọn vẹn sức mạnh của Gutenberg Editor (như Block Inserter, Inspector) trong một môi trường "vô trùng" (không xung đột CSS, không rò rỉ RAM do bị React unmount khi đóng Modal).
- **Decision (Zero-Postmeta & Title Stripping Interception):** Bất chấp việc chạy Iframe gọi `post.php` của một Shadow CPT thực thụ, một cơ chế **Interception Layer** (đánh chặn) trong `Organism_Editor` đã được dựng lên. Để bảo vệ tuyệt đối tên của Symbol (không bị ghi đè bởi "Ska Dummy Editor"), CPT ảo này đã bị tước bỏ hoàn toàn thuộc tính `title` (`'supports' => array('editor', 'custom-fields')`). Cơ chế Intercept chặn đứng hoàn toàn việc ghi đè rác vào `wp_posts` hay `wp_postmeta`, thay vào đó nó sẽ điều phối Dữ liệu (HTML & JSON) lưu thẳng xuống Database dạng phẳng `ska_data_sys_organisms` (Ska Data Pro), đồng thời ngăn cản việc ghi đè trường `name` ở cơ sở dữ liệu và Cache.
- **Decision (PostMessage Bridge):** Thiết lập một cầu nối đồng bộ thời gian thực giữa Iframe và Cửa sổ cha thông qua `window.parent.postMessage`. Ngay khi người dùng lưu thành công bản mẫu gốc trong Iframe, cửa sổ cha tự động cập nhật Cache tĩnh (`window.skaOrganismsCache`), tự đóng Modal và Render lại giao diện mà không cần refresh trang.
- **Decision (JIT Compiler Cache Invalidation):** Sửa lỗi JIT Compiler không nhận diện CSS mới ở ngoài Frontend sau khi người dùng Sửa Bản Gốc. Lỗi do cơ chế lưu Database của WordPress thông qua `ska_data_update_record` không gọi Hook để xóa RAM Cache (Transient), khiến JIT Compiler luôn quét nội dung cũ. Bổ sung Hook `ska_data_row_updated` để System Cache tự động nhận biết, giải phóng RAM và xuất lại file JSON mới ngay lập tức. Trang bị thêm Iframe UX Loading Spinner giúp không bị trắng trang khi tải giao diện biên tập.

## 2026-04-20 - 🟢 Ska Symbols (Phase 4.1): Editor UI & Zero-Query
- **Decision (Ghost Block Architecture):** Hoàn thiện luồng React UI cho Ska Symbols bằng cách bổ sung `ska-organism-ref` vào quy trình Build Webpack. Thay vì nhân bản HTML, block tham chiếu này chỉ giữ đúng khóa ngoại `organismId`. Quá trình giao tiếp với Database qua API cũng được củng cố bằng việc chèn chính xác Schema `html_content` để hứng Full Dữ liệu.
- **Decision (Zero-Query Inserter):** Áp dụng kỹ thuật tối ưu Zero-Query cực độ cho Editor Inserter và Inspector bằng cách `wp_localize_script`, nạp sẵn JSON Cache của file `organisms.json` thành biến toàn cục `window.skaOrganismsCache` thẳng vào trình duyệt. Đảm bảo UI SelectDropdown mượt tuyệt đối (0ms latency), cắt đuôi dependency vào Fetch API bất đồng bộ và triệt tiêu gánh nặng Database cho trang Admin Canvas cực nặng.
- **Decision (Real-time Cache Injection - 2026-04-22):** Giải quyết dứt điểm rào cản SPA (Single Page Application) của Editor. Khi người dùng bấm "Save as Organism", API POST Database thành công sẽ không yêu cầu tải lại trang. Hệ thống ứng dụng cơ chế **Data Injection** tiêm ngược dữ liệu trực tiếp vào `window.skaOrganismsCache` ở màn hình Client. Từ đó tính năng Local Edit (Phân rã - Detach) tức thì hoạt động hoàn hảo 0ms delay.
- **Decision (ServerSideRender Fix 2026-04-21):** Sửa lỗi "Block rendered as empty" trong môi trường Editor. Do hạn chế của cơ chế Output Buffering trong hàm `render_block` (WordPress 6.1+), file `render.php` của block tham chiếu bắt buộc sử dụng lệnh `echo` thay vì `return` để React UI có thể hứng trọn nội dung HTML. Đồng thời cập nhật whitelist attributes trong `block.json` để loại trừ hoàn toàn lỗi 400 Bad Request từ REST API.

## 2026-04-20 - 🟢 System Finalization: Packaging & Dashboard UI
- **Decision (Plugin Packaging Strategy):** Tinh chỉnh cấu trúc Build ZIP cho Plugin. Lọc bỏ 100% `node_modules` và `src` lẩn sâu trong các thư mục con (ví dụ: `skafx-editor`) giúp plugin `ska-logic-engine` nhẹ hơn gấp 250 lần (từ >100MB xuống 0.38MB), tối ưu tuyệt đối cho công tác sao chép, cài đặt trên server thực.
- **Decision (Ecosystem Warning Banner):** Thực thi nghiêm ngặt UI chặn lỗi phụ thuộc. Nếu `Ska Data Pro` hoặc `Ska Logic Engine` chưa được kích hoạt, hệ thống sẽ chèn tức thì Red Banner tại Admin Dashboard giúp định hướng người quản trị kích hoạt ngay 2 thành tố sống còn của Ecosystem.
- **Decision (Physical JSON Caching cho Design Tokens):** Bảng phẳng `ska_data_sys_presets` giờ đây không chỉ lưu trữ tại MySQL mà còn tự động sinh tệp tin Physical JSON Cached File tại `wp-content/uploads/ska-data/tokens.json`. Quyết định này giúp JIT Tailwind giải phóng hoàn toàn DB Queries để tăng tốc độ parse & render CSS lên cực độ. Xóa bỏ hoàn toàn tình trạng lỗi 500 khi lưu cấu hình do sai cột dữ liệu.
- **Decision (Zero-Trash & Documentation Pipeline):** Tái áp dụng triệt để chính sách dọn dẹp và duy trì bộ nhớ tài liệu. Cập nhật `system_map.md` và `decision-log.md` an toàn để kết thúc Phase 3 - Kiến trúc cơ sở hạ tầng.
- **Decision (End-User Manual):** Hoàn thiện bộ tư liệu Hướng dẫn dành cho End-User (lưu tại `docs/`), phân tách triệt để tài liệu kỹ thuật Dev và Hướng dẫn kéo thả. Giữ hệ thống gọn gàng.

## 2026-04-19 - Brainstorm: Ska Symbols & Smart Object 'app-site'
- **Decision (App-site Tables):** Trong Smart Object `app-site` sẽ chứa các bảng quản lý Thông tin trang, Theme builder, Custom block, và Preset setting. Điều này giúp tận dụng tối đa hệ sinh thái Ska Data Pro để lưu trữ và quản lý cấu hình hệ thống mà không cần đẻ quá nhiều CPT nội bộ.
- **Decision (html2tailwind Custom Colors):** Dự kiến ở Phase tiếp theo sẽ bổ sung cấu trúc lưu mã màu tùy chỉnh khi dán HTML code khối lớn từ Tailwind/Flowbite vào Editor, nhằm biến hóa Ska No-Code Design Engine mạnh mẽ hơn.
- **Reference:** Kế hoạch chi tiết đã được đăng tải và suy xét kỹ lưỡng trên Notion.

## 2026-04-19 - 🟢 Bugfix & UX: Tối ưu UI Offcanvas (Ska Builder)
- **Decision (Offcanvas Close Button Positioning):** Thay vì để nút Close nằm chơi vơi ngoài panel dễ bị ảnh hưởng bởi z-index cục bộ của cấu trúc Edit Mode, nút Close được đẩy vào trong Panel (position: absolute) cùng `z-index: 9999`. Việc này ngăn chặn sự cố các khối nội dung bên trong Gutenberg (thường tự sinh position relative và z-index khi được chọn) vô tình đè vùng nhận click, làm liệt chức năng tắt Offcanvas.
- **Decision (Offcanvas Content Safety Padding):** Giảm thiểu xung đột layout bằng cách tăng `pt-16` (padding top) cho container nội dung của Offcanvas, đảm bảo người dùng Nocode khi kéo thả Text/Image sẽ không bị chữ đè lấn lên khu vực góc phải phía trên của nút Close.

## 2026-04-18 - 🟢 Nâng cấp UX: Skapine Engine Auto-fill & Phân nhóm HTML Attributes
- **Decision (HTML Attributes OptGroup):** Tái cấu trúc Panel nhập "Thuộc tính HTML/Alpine" bằng cách sử dụng thủ thuật Placeholder-disabled để mô phỏng thẻ `optgroup` (DO Gutenberg SelectControl không hỗ trợ native optgroup). Nhờ đó, người dùng Nocode có thể dễ dàng nhận dạng và chọn lựa giữa 4 nhóm chính: Hiệu ứng/Animation (chạy trong ranh giới Preview), Chức năng Data/Logic (chạy ngầm), Tùy chỉnh Form và HTML Cơ bản.
- **Decision (Smart Auto-fill Defaults):** Khắc phục vấn đề người dùng No-code lười gõ lệnh Alpine JS bằng cách thiết lập Cơ chế Điền tự động (Auto-fill). Khi người dùng thả thuộc tính (VD: `@mouseenter`), hệ thống ngay lập tức bơm sẵn logic mẫu (VD: `hover = true`). Khi chọn `x-data`, tự sinh JSON `{ open: false, hover: false }`. Lối thiết kế chủ động này tiết kiệm 80% thời gian thiết lập hiệu ứng động.
- **Decision (Skapine Engine Preview Mode - Mouse Events):** Bổ sung trình duyệt sự kiện (Event Listeners) cho `onMouseEnter` và `onMouseLeave` trực tiếp vào nhân Skapine (`withSkapine.js`). Cho phép khối Block trong trạng thái kích hoạt Editor Preview Mode (viền vàng) lập tức phản hồi và Play Animation (VD: xổ Dropdown, thẻ Tooltip) khi người dùng No-code rê chuột ngang qua mà không cần mở tab Frontend. Cơ chế tách biệt (`startsWith`) giúp hỗ trợ cả Modifier phức tạp như `@click.prevent`.

## 2026-04-16 - 🟢 Done - Phát triển Ska Molecules: Khóa Xương Sống & Alpine.store (Phase 4)
- **Decision (Ska Multi-Step Quiz/Wizard):** Vươn lên trở thành Native Variation (`ska-molecule-multi-step`) bám trên Component Root là `ska-builder/container`. Thay vì bọc thẻ form rườm rà, container tự động transform thành `<form isSkaForm="true">`.
- **Decision (Tailwind Compiler For HTML Attributes):** Giải quyết triệt để lỗi "x-transition không hoạt động". Không bắt người dùng NoCode phải "thủ công" rải các thẻ Snail-trail (Chim mồi) tàng hình nữa. Cập nhật `extract_block_classes()` của Tailwind Backend Compiler để tự động móc nối toàn bộ các attributes trong mảng `htmlAttributes`. Tiến xa hơn ranh giới CSS tĩnh, chính thức sát nhập thế giới Animation/Interactions của Alpine.js vào chu trình Build tự động của Ska.
- **Decision (Ska Molecule Animation JIT):** Đã sửa đổi JIT Compiler để đáp ứng class `transform` và hỗ trợ đầy đủ bộ công cụ hiệu ứng Thu/Phóng (Scale, Translate) cho AlpineJS. Cho phép biên dịch thời gian thực ngay lập tức các hiệu ứng chuyển cảnh siêu việt mà không cần Snail-trail.
- **Decision (Prevent Event Fallback):** Bổ sung trực tiếp modifier `@click.prevent` vào Menu chọn thuộc tính. Cứu rỗi No-code users khỏi thảm họa gắn hiệu ứng vào thẻ Links/Buttons bị ép tải lại/nhảy lên đầu trang. Tất cả giao diện động (Tabs, Dropdowns) của Ska Module giờ đã ổn định và độc lập.

## 2026-04-14 - Decision: Pivot khỏi Alpine Logic Injection (Ska Molecule)
- **Decision (Bãi bỏ Code Injection):** Hủy bỏ tham vọng ép người dùng Nocode phải cấu hình sự kiện `$ska.submitForm` thông qua AlpineJS Attributes tại Frontend. Nhận thức việc hiển thị chuỗi Javascript (fetch API, payload JSON) trong Inspector là rào cản (Deal-breaker), tiêu diệt hoàn toàn trải nghiệm No-code.
- **Decision (Khôi phục Form Cải Tiến):** Tạm dừng luồng tích hợp "Ska Molecule x Logic Engine" thông qua Custom Attributes. Chuyển hướng sang phát triển "Ska Form Builder Cải Tiến": tự động hóa quá trình thu thập payload và gọi ajax. Các tính năng AlpineJS (x-transition, x-show) được bảo lưu toàn vẹn, nhưng chỉ giới hạn dùng cho UI (Tab/Modal/Dropdown/Animation), không ép user gõ JS.

## 2026-04-12 - Decision: Logic UI DB Picker & App Auto-Grouping (Logic Engine)
- **Decision (Modal DB Picker):** Loại bỏ hoàn toàn thẻ `<datalist>` dropdown cũ của giao diện thiết lập "Insert/Update Record" tại Logic Builder. Thay thế bằng Hệ thống Modal UI Picker có hỗ trợ Real-time Search Box. Lựa chọn này giúp trải nghiệm Nocode trở nên tin cậy hơn, tránh thảm họa đánh máy nhầm (Typo) làm rớt cấu trúc MySQL.
- **Decision (Strict App Grouping & Extraction):** Bác bỏ phương án dùng Regular Expression (`Regex`) chẻ chuỗi để đoán Tên Nhóm App (vì gặp rủi ro gãy chuỗi do người dùng đặt tên App ID có chứa quá nhiều dấu gạch ngang, VD: `app_kiem_thu_moi`, hoặc chứa tiền tố hệ thống `wp_`). Kiến trúc được chuyển sang trạng thái móc ruột lấy khóa ngoài (Foreign key) `$meta['__table_info']['app_id']` từ lõi `Ska Data Pro` và quy chiếu chéo sang Option RAM `ska_data_apps`. Trạng thái này đảm bảo phân nhóm DB không thể sai lệch dù có thay đổi tiền tố hệ thống wpdb.

## 2026-04-12 - Decision: Ska System Framework & Lộ Trình Mới Phase 3
- **Decision (Khai Tử Master Plugin):** Quyết định hủy bỏ vĩnh viễn dự án `ska-no-code-home` plugin. Chuyển đổi phương pháp thiết kế System Dashboard thông qua mô hình "Shared Drop-in Framework" (Thư viện dùng chung nhúng thẳng vào các plugin nhỏ) để hệ sinh thái gọn nhẹ và chống xung đột. Cơ chế Load-Balancer tự nạp Dashboard duy nhất ở Wp-Admin.
- **Decision (8-Step Roadmap Phase 3):** Lập lại thứ tự tấn công để đạt App Portals Builder mà không ngã ngựa: (1) Logic Engine CRUD [Xong Update] -> (2) Logic UI DB Picker -> (3) Ska System Framework Dashboard -> (4) Ska Molecule & Alpine.js Integration -> (5) Custom Block/Symbols -> (6) Theme Builder/App Portals -> (7) Auto-Generated CRUD Portal -> (8) Frontend Triggers Nâng Cao.
- **Decision (Action Node CRUD):** Đã bổ sung xong `Ska_Update_Data_Action` (Node Cập nhật). Vẫn trung thành 100% với kiến trúc Decoupled Boundary (Goi Hook `ska_data_update_record`), giao nhiệm vụ Insert/Update SQL trực tiếp cho `Ska Data Pro` giải quyết, không can dự $wpdb ở phân hệ Logic Engine.


